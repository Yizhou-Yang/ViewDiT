# 第三轮：语义非干扰与双参考统计传输

## 动机与不允许的论证

上一轮只证明了归一化串扰可以消除，未证明顶会级应用收益。本轮不把更多MSE配置或方法改名当novelty。

三项研究对象：
1. 双参考边界统计传输：保护区固定参考统计，编辑区使用原始编辑decode的统计；比较全局参考固定及渐变版本，防止强编辑分布偏移。
2. 真实CogVideoX-2b文本条件去噪产生编辑latent，VAE维持官方FP32。原模型及基础采样不训练，不冒充专门训练的视频编辑模型。
3. 再使用成本与闭环检验：像素拼接不强制丢弃原latent。必须包括“像素拼接＋保留编辑latent”的强基线；只和强制重编码比较不能证明方法价值。

## 本轮新方法

每层/每次归一化调用记录参考统计与原始编辑统计。a=gamma/sigma，b=beta-mu*a。输出使用时间相关的仿射混合：

    y(t) = ((1-m(t))*a_ref + m(t)*a_edit)*h(t)
           + (1-m(t))*b_ref + m(t)*b_edit

m=0的保护前缀不能从未来获得统计信息。其余算子的因果性和latent前缀冻结仍是保证的前提。默认分批下17帧最后latent边界为13；后续33帧最后边界为29。实现明确检验调用及时间分辨率映射，不宣称任意空间mask或任意块内边界。

双参考增加一次edited decode统计捕获；不能只报最终decode耗时。方法不是标准decoder或无side-information原生latent。参考信息来自合法输入，不含保护未来的目标泄露。

## 开发试验

4条已用内容bmx-trees/boat/bus/dog，17帧，224×128，末latent0.4/1.0几何扰动，reference/dual/dual_ramp三方法，共24配置。此处是开发，不是独立持出。

预定选择依据：前缀相对零编辑误差≤1e-6；在幅度1下编辑相对原始decode RMSE明显低于全局固定，同时保留幅度。像素拼接在两端误差为0，方法不能仅靠相对旧版提升就宣称领先拼接。

## 语义验证

先做资源兼容检查，不能将低分辨率smoke当正式语义成功。优先采用CogVideoX原生480×720以及模型合法帧数；17帧用于一次资源检查，正式目标49帧。V100若不能承受需保留报错、分辨率与实测时间，不悄悄切小分辨率后声称原生成功。

固定四条内容和文本目标：
- bear：棕熊变为北极熊，第二轮改为熊猫。
- blackswan：黑天鹅变为白天鹅，第二轮加暖色夕阳光照。
- bus：公交车变为红色，第二轮变为黄色。
- boat：帆船变为红色，第二轮加夕阳光照。

一次有效底座编辑并不保证第二次；报告全部样例和筛选率。先使用单个bear场景作工程smoke，之后不得把该样例当独立测试。

采样：官方CogVideoX DDIM scheduler，固定共用初始噪声；CFG6；50步总时间表，编辑strength0.6，预计30次有效denoise。最先一次兼容性smoke可4步，不作语义结论。FP16 transformer，FP32 VAE；文本encoder在预计算embeddings后卸载。记录encode、DiT、decode、额外统计捕获及模型加载分别耗时。基于原视频latent的mode编码，避免不同方法后验采样噪声不同。

比较标准decode、参考统计、双参考统计、像素拼接、拼接重编码。所有decoder方法使用同一真实DiT编辑latent，不为各方法重新抽噪声。像素拼接＋保留编辑latent作为有力再编辑基线独立列出。

指标：保护最大像素误差、编辑区域相对原始语义结果RMSE及人工目标核查、编辑幅度、再次输入encode后的latent漂移、重新decode的核心/保护误差、真实端到端成本。不能从小RMSE推导语义成功或自然过渡，不能用单一CLIP值代替盲评。

## 论文规格与门槛

数学贡献需是实际控制图/条件保证与可验证反例，而不是把AdaIN、满秩Jacobian或逆函数定理重新包装。

实验规格目标：≥3种结构不同VAE、≥2种真实语义生成/编辑底座、≥30个独立内容、2轮及以上编辑、预先固定难度和失败标准，视频级配对区间，盲评和相同总预算。具体门槛属于研究设计，不是保证接收的规则。

决定性门槛仍是对像素拼接＋latent复用的实际优势。若该基线提供相同输入latent和同等或更佳渲染，不能用“无需重编码”做独有贡献。必须找到并验证更自然的模型内过渡、模型内梯度控制或新的可解释任务收益；否则将该路线降级为机制/工程研究，不声称顶会方法已成立。
