# 块内时间控制预注册与方向判据

日期：2026-09-21。状态：新实验运行中，未读取结果。

## 要排除的偷换

1. 原生latent可以用标准VAE解码，不等于该latent仍在DiT可接受分布上。需完整再编辑测试。
2. 增加解码器特征残差后得到的是sidecar扩展表示，不是原生latent。不能沿用原生兼容性主张。
3. 像素目标本来就能拼接精确达到。MSE比像素拼接差的方法，价值只能来自可验证的表示、存储、可组合性或后续任务，不能人为禁止像素基线。
4. 因果保护只对未受未来输入影响的前缀成立，不证明后缀保护，不证明任意双向VAE。
5. 同迭代数不等时延；特征缓存、PCA构造、编码都应单列。

## 试验一：块内边界

既有8个真实视频实例，每个17帧。编辑latent2名义影响5至8帧；要求保留5、6帧编辑，从7帧开始恢复原视频。因此编辑终止点位于同一latent块内部。

先用固定排序前两例检查实现与初步效应，不按结果挑样。若方法有信号，再运行剩余实例。此数据已反复用于探索，不叫独立测试集。

基线：不补偿；拼接后重新编码再替换latent2以后；同32步原生latent优化，核心损失权重1/10/100；oracle像素拼接；与低秩特征残差控制参数数量相近的低分辨率像素残差。

候选：在最后时间上采样后，以rank8特征基和低分辨率逐帧系数注入残差，从帧7起生效，避免影响帧0至6。随机正交基与每样本PCA基分别对比。冻结全部权重，Adam32步，学习率0.03，无best-of选择。

度量：真实解码保护区MSE；核心相对RMSE和幅度；最大前缀偏差；参数数；耗时和显存；带sidecar完整解码一致性。

## 选择标准

- 如果native块内优化能在核心相对RMSE<=1%条件下稳定降低保护误差50%以上，继续原生约束优化，但仍不能把Adam当创新。
- 如果native不行，而sidecar能硬保持前缀并在同参数像素残差下显著更好，可选择稀疏时间分辨率扩展表示，必须承认非原生latent。
- 如果sidecar仅靠大量参数、逊于像素残差或只是显式拼接，则不升级成方法贡献，转而提炼关于表示兼容性与精确编辑的可检验研究问题。
- 所有候选必须保留失败实例。不能为了宣布顶会潜力而降低基线要求。

## 近邻风险

IV-VAE(CVPR2025)已研究因果时间压缩导致帧间性能不均衡。
RefDecoder已研究参考信息绕过压缩latent注入decoder。
LDF-VFI已使用多尺度条件encoder与残差注入decoder。
因此“压缩损失细节”“因果帧不均衡”“解码器加条件”都不能作为首次贡献。
