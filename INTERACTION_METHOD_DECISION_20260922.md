# 交互方法：GPU迭代后的停线判断

日期：2026-09-22。

## 决策

停止把“二值单项残差+交互残差线性头+四世界监督”作为主论文方法。不扩大到昂贵的DiT训练，不把loss改写或三分支命名当novelty。两轮GPU训练未通过主指标门槛，而且该具体结构与普通多任务直接头函数类等价。

这是对当前具体方法的研究投入判断，不是证明整个条件交互研究或视频编辑方向不可能成功，也不是穷尽超参数后的最优性结论。实体共享、非线性操作条件响应尚未实现，不能声称本实验已经证伪它们。当前没有已确认足够成为顶会成果的新方法。

## 1. 实际运行

Tesla V100-SXM2-32GB，torch 2.6.0+cu124。新建完整解析三维渲染与Conv3D训练，不是复用单bear训练显存探针。

每轮200训练场景、40验证场景、80测试场景。每场景5帧48x64，原视频+删除A/删除B/删除两者的三个目标。训练和测试不重叠，第二轮换全新的测试seed区间。两类模型各161097参数，同backbone、相同初始零输出、相同batch序列、Adam、batch4、3训练seed、2学习率，验证集单独选checkpoint和学习率。

共24次训练配置、28800个optimizer step。第一轮12配置×600步；第二轮12配置×1800步。第二轮双方都增加相同的changed/effects/keep区域归一化MSE及0.1交互MSE，非只给候选额外监督。没有下载或修改基础模型。

直接模型输出三个编辑残差rA,rB,rBoth。候选输出rA,rB,rAB并以rBoth=rA+rB+rAB合成。都同时预测三种操作。没有自然语言编码、扩散采样或预训练视频先验。

渲染：sphere/plane射线交点、Lambertian直接光照、四方向光可见性、解析地面纹理、规定轨迹。不是独立二维贴图加阴影。重复渲染最大差0；第一轮45/80场景、第二轮36/80场景存在阈值以上交互信号。

限制：固定相机和光源、两个球、无间接光/镜面/动力学/抗锯齿，随机球可能互穿；全部三种操作组合训练可见。仅验证该简单原型的跨场景参数泛化，不是未见操作组合、资产家族或真实视频泛化。不得称为完整物理数据集或顶会主表。

## 2. 测试结果

下表为候选相对直接头的误差下降百分比，负数为更差。CI为2000次成对seed+scene bootstrap，只有3个seed，均为探索性区间，没有多指标校正。

| 指标 | 第1轮下降 | 第1轮95% CI | 第2轮下降 | 第2轮95% CI |
|---|---:|---|---:|---|
| 全图MSE | 6.08% | [-3.45%,16.06%] | 5.80% | [-27.15%,27.12%] |
| 必要变化区MSE | 8.41% | [-4.52%,20.57%] | -7.22% | [-24.66%,5.63%] |
| 效果区域MSE | -15.57% | [-31.73%,-0.32%] | -12.41% | [-23.17%,-4.22%] |
| 保持区域MSE | -26.01% | [-93.47%,8.74%] | 11.55% | [-30.27%,33.69%] |
| 交互差分MSE | -23.59% | [-58.08%,-3.98%] | 20.82% | [-12.67%,40.94%] |
| 时间差分MSE | -2.05% | [-7.61%,1.09%] | 2.46% | [-2.19%,5.70%] |

第二轮核心原始值：效果区域直接头0.000620266，交互头0.000697259；必要变化区直接头0.001351339，交互头0.001448849。

不是两者完全没学会：第二轮identity的效果MSE为0.09842394，直接模型降至0.00062027；必要变化区从0.07527549降至0.00135134。但仍有局部残影/边缘误差，不能宣布达到感知完美。

只取有交互信号的36个第二轮测试场景（事后描述性检查，不作为新显著性检验）：效果MSE直接头0.00068746、候选0.00074533，候选仍较差。交互MSE候选0.00037028、直接头0.00045028，不能据此忽略效果目标与CI。

重要风险：第二轮候选/直接头的全图交互MSE均高于identity对应值0.00016667。原视频不编辑当然不正确，但说明较低的单个对比差分误差不能代替正确编辑。必须同时报告干预正确性与保持，不能只挑20.82%这一格。

同参数/同样本/同步数已控制。候选额外加法略有成本；计时包含validation，部分时段与只读GPU检查重叠，不能用这些运行时间声称精确等GPU秒或速度收益。

## 3. 实测等价性与novelty

普通头参数可写为：WBoth=WA+WB+WAB，bBoth=bA+bB+bAB。反向变换做减法。保持backbone不变即可将任何已训练候选转换成普通头。

GPU检查全部测试场景：第一轮三种seed最大输出差不超过3.58e-7；第二轮不超过2.69e-7。无需重新训练。这比仅在随机向量上验证恒等式更直接，但适用范围仍仅是共享backbone+线性输出头+二值操作的当前实现。

函数类等价不表示Adam轨迹等价：非正交坐标变换会改变优化几何。即使观察到收益，也要解释为优化/归纳偏置，而不是创造了普通头无法表达的物理交互机制。此前四世界正交loss等价审计进一步排除了完整等权对比MSE本身新增信息的说法。

最近邻核对：

- FactorMatte已有条件跨层交互与反事实分解，不是朴素独立层。
- Generative Omnimatte已有源视频、mask、clean-plate和solo条件生成；官方页面明确包含多对象Kubric、保留/移除trimask，并公开多对象难例。我们没有复现它，不能声称它已在本协议失败。
- AlbedoEdit用源RGB、原始/编辑首帧albedo、普通flow-matching进行统一多任务训练，覆盖隐式阴影与反射协调。
- VOID已训练物理交互删除，不能用“反事实/因果”几个词定义空白。

当前候选与上述工作的具体接口不完全一样，但“不是完全相同实现”远不足以证明有重要新贡献。方法层等价与实测未过门槛共同支持停线。

## 4. 换向方式

优先更换方法假设，而不是回到GN压缩，也不凭空宣称另一个方向必成顶会。

下一可检验问题：同一源视频产生多个编辑分支时，所有分支是否共享同一个可见内容解释及遮挡补全假设？删除A、删除B和删除AB分别看都合理，但共同暴露的背景、另一对象的纹理和身份可能互相矛盾。目标从“把平均输出写成二阶残差”改为“学习多个编辑结果的联合条件分布及共享隐变量”。

候选技术路径：源视频推断共享场景后验状态，各操作分支读取该状态；在允许变化的部分保留操作特异噪声，对跨分支同一实体的公共可见区域施加对应关系约束。条件应来自可观测的源视频，不在测试注入真值geometry。遮挡不确定时保留多种共享假设，不能通过输出同一模糊视频获得伪一致性。

但这仍是候选，不是确认novel：DCM、DiffusionCounterfactuals、ICML2025 Semantic Abduction已覆盖外生噪声/语义身份的反事实推断；共享噪声、参考attention、视频memory本身也不新。必须进一步证明独立编辑+相同seed、共享reference/memory、联合多输出生成和同监督普通模型不能达到同样结果。

最小成功信号应是相同编辑正确率/质量/采样预算下，联合一致性与遮挡补全校准提升，且不能靠减少多样性取巧。需要独立模型验证器、真实视频和未见组合；单个差分指标不再足够。当前本轮未运行这个新方法，不能把上述Conv3D结果挪作其正面证据。

## 5. 资产与状态

本地原始结果：interaction_gpu_pilot_stats.json、interaction_gpu_pilot_v2_stats.json、interaction_gpu_pilot_v2_inspection.json。

代码：interaction_gpu_pilot.py、interaction_gpu_pilot_v2.py、interaction_pilot_inspect.py。复现：INTERACTION_PILOT_README.md。接触图使用固定首4例、seed11、固定中间帧，非按质量挑选。远端两个独立结果目录保留模型和预测。未覆盖旧实验，未commit/push。

本轮完成的是“当前具体候选的GPU迭代与停线判断”；没有完成一篇新顶会方法的证明或真实视频SOTA复现。

## 来源

- FactorMatte：https://factormatte.github.io/
- Generative Omnimatte：https://gen-omnimatte.github.io/
- Generative Omnimatte论文：https://arxiv.org/html/2411.16683v1
- AlbedoEdit：https://arxiv.org/abs/2606.01362
- VOID：https://arxiv.org/html/2604.02296
- Semantic Abduction，ICML2025：https://icml.cc/virtual/2025/poster/44995
- Diffusion-based Causal Models：https://arxiv.org/html/2302.00860v3
- DiffusionCounterfactuals：https://ar5iv.labs.arxiv.org/html/2407.20553
