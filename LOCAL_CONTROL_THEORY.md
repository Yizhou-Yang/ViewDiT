# 时间局部编辑的条件可控性

日期：2026-09-21。本文为推导与待检验命题，不代表已完成顶会贡献。

## 核心对象

固定视频VAE、解码执行配置、已确定的前缀状态h，令u为跨越编辑终止点的当前latent块。该块的输出分成要锁定的编辑帧E与要恢复的保护帧P：

f(u)=D_E(h,u)，g(u)=D_P(h,u)。

J_E与J_P为在当前u附近的Jacobian。此前仅修改未来块的因果保证仍有效，但未来块不能改变当前块的帧。

## 命题一：局部精确锁定可能消除块内控制自由度

若f在u邻域连续可微且J_E(u)满列秩，则存在u的某个邻域，在该邻域内f(u')=f(u)蕴含u'=u。

证明：满列秩可选择与输入维度相同的输出坐标，使相应方阵Jacobian可逆；对这些坐标使用逆函数定理，得到局部一一对应。完整输出相等包含这些坐标相等，故输入相等。

结果：在该邻域精确锁定E，就无法同时改变g。这里不是“无解”于全局；远处别解、修改更早latent、改变decoder、额外sidecar、放松像素精确要求都在命题外。

## 命题二：近似锁定下的局部变化界

在线性化模型里，如果J_E满列秩，则对任意delta：

||J_P delta|| <= ||J_P J_E^dagger|| ||J_E delta||。

因此核心变化预算epsilon限制保护帧的局部可改变量。对非线性D应用该界必须添加Taylor余项或给出信赖域；未经余项控制不能称认证不可达。

实际可计算的目标前沿：

min_delta ||r_P + J_P delta||^2
subject to ||J_E delta|| <= epsilon, ||delta|| <= rho。

需要同时报告线性预测与真正D(u+delta)的结果，阈值/归一化/步长公开。一个抽样子空间满秩只证明这个子空间；不能推断全维空间。

## 命题三：完整计算图决定因果性

仅有causal convolution不足以推出完整decoder因果。若GroupNorm的均值方差在时间维上联合统计，较晚帧会通过统计量改变较早帧。按decoder分批时，这条依赖还可能受分批边界影响。

实验必须包含：同一latent在不同合法分批配置下的零编辑差；未来latent扰动下的前缀变化；固定参考统计后的因果响应；不能把分批改变后的质量变化当免费修复。

## 方法如何由分析导出

当用户给定E/P/B三类帧后：先确定完整decoder的影响关系；冻结对E有影响且无法安全更改的控制块；只在剩余变量优化P；若预算不满足，返回所测试过渡长度与实际误差前沿，而不是悄悄削弱E。

对存在跨时间归一化的decoder，可以在固定参考解码统计下测试消除额外反向串扰。但这是带参考状态的decoder执行，不是普通latent兼容，必须报告统计存储、编辑失真与零编辑偏差。

## 与近邻的关系

PELC/DecFormer已经给出像素等价latent合成与残差学习，且论文理论附录包含局部二次Jacobian。我们不能声称首次做Jacobian或像素等价。差异若成立，应是：在硬锁定部分时间输出的约束下刻画同块latent局部可识别性，并据此决定最少过渡支持或是否必须增加表示自由度。

LFV已经给出VAE特定频域响应、有效性筛选、往返漂移与跨模型前沿。我们不能把“评估可行性再选择算子”本身当新颖点。差异若成立，应由不平移不变、单个时间边界、锁定核心和因果状态所造成的结构性限制及有效干预体现。

IV-VAE已经观察帧间不均衡，VideoCanvas/EasyV2V已有精确时间条件，RefDecoder已有条件decoder，故实验仅发现这些现象并不足以发方法论文。
