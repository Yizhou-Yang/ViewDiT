# ViewDiT回到生成起点：研究协议

2026-09-22。用户最新目标：在DiT生成过程中提高生成质量或加速，training-free或小训练均可，V100可迭代。允许组合已有组件，只要整体贡献与已有工作有足够区分且收益显著。不会以视频编辑、记忆、VAE参考修复替代这一目标。

## 研究目标与不当硬约束的取消

目标是推高真实质量-延迟Pareto前沿，而非追求某个张量指标、每个零件原创，或无论如何声称SOTA。允许复用预测缓存、部分计算、LoRA和蒸馏。已有组件不自动否定组合方法；已有组合的存在也不自动否定具有新机制且效果更好的迭代。录用和前所未有无法提前保证。

V100提供真实算法迭代，H20验证作为后续迁移而非本轮结果。不能用V100不支持的新架构FP8/低比特kernel推算本轮速度。小训练的总GPU小时、教师生成、搜索与校准成本均需报告。

## 主线建议

固定预算下的联合计算分配与小规模适配：使用成熟加速组件作为执行器，重点解决多个近似同时出现时局部最优不等于生成轨迹全局最优的问题。候选目标是根据联合影响而不只是各组件单独误差，决定步、层或分支的计算预算；必要时再训练适配器补偿组合部署产生的分布偏移。

这里只是待验证的整体研究框架。联合优化、误差累积、低秩表示、LoRA均不是新发明。主贡献必须精确落到更好的联合影响估计/优化或不同的训练机制，且在同预算简单组合、进化搜索和已有联合训练方法前有稳定优势。

不立即同时堆叠四个组件。先选两个成熟执行器和一个统一目标，逐级验证单项、朴素叠加、联合策略、再加小训练。端到端质量不达标时，先查采样与执行路径，不能由错误配置直接证伪方向。

## 创新空间边界

- TaylorSeer/HiCache/RFC：预测缓存及输入输出关系预测已充分研究，重命名外推不是贡献。
- FasterCache/OUSAC：缓存与CFG联合节省、优化guidance schedule已有先例。
- CacheQuant/ERTACache/TCC/CEM：组合误差、轨迹漂移、误差校正和全局调度已有先例。
- HarmoniCa/OnlineCache/DiffSparse/E-DiT：小训练router、误差corrector、端到端质量目标、步骤/层/宽度等联合分配已有近邻。
- VSA/SLA：稀疏注意力与小训练已可显著加速，但需要核验V100 kernel，不假设纸面FLOPs就是时延。
- STG/APG/autoguidance：质量提升的强基线。若用省出的预算换guidance，必须对比简单组合，不能把额外计算当免费收益。

CacheQuant(CVPR2025)、SLA(ICLR2026)说明“组合能构成成果”，不是说我们复制这些组合就有新意。文献表述优先作者论文/官方来源，不沿用搜索摘要中的夸张数值与错误架构描述。

## 第一轮实际机制实验

脚本：dit_compute_allocation_pilot.py。
纯噪声CogVideoX-2b T2V，不输入源视频、mask或历史。17帧480x720；DDIM30、CFG6、eta0；FP16 DiT、FP32 T5，解码后独立FP32 VAE进程。不是完整49帧设置。

cal_water：白天鹅缓慢游泳，seed2026092301，用于校准。
test_street：红公交车街道行驶，seed2026092302，用于开发级迁移，不用于选策略。

4个相同规模block组：3..8、9..14、15..20、21..26（0-index）。指定组在step5,7,...25复用前一步本组的真实block残差，视频和文本流均保留当前输入并加缓存残差；其他位置正常计算。缓存不跨样本、CFG分支或实验配置混用，缓存本身按batch保存。

每prompt配置：全计算；4个单组；6个双组；26步全计算。全部同一个初始Gaussian张量。

全计算900次block调用；单组834；双组768；26步780。该计数一次包含conditional/unconditional两条batch，不能称为900个独立完整DiT NFE。采样步数仍30；不是把少跑代码藏在计数中。

校准上每单组终点位移d_i=x_i-x_full，计算G_ij=<d_i,d_j>/D。对任意双组，独立预测G_ii+G_jj，联合预测G_ii+G_jj+2G_ij，分别选一组并冻结。两个选择都只依赖校准单组输出。测全部pair是检查预测与真实组合误差，测试oracle仅诊断，绝不可作为部署策略。

公式来自误差向量内积展开，是标准线性化/二次风险，不是新理论。在误差大时d_AB不等于d_A+d_B，需直接报告非线性残差。跨prompt符号不稳定时不能宣称可迁移误差抵消。

第一轮门槛：联合预测需要实际改变策略且在开发迁移上优于独立策略，才说明此简化机制有初步收益；同一策略或结果较差则不做正面结论。但一个校准prompt和一个迁移prompt仍不能确定最终方向，可能需要更丰富统计或不同执行器。

## 正式成果门槛

1. 同一基础模型、条件、seed、采样器，报告端到端时延/峰值显存及采样时延；warmup与重复测量明确。单组/双组实际不同缓存显存也要计入。
2. 用不少于16个独立prompt起步，prompt级配对统计；开发、校准、封存测试严格区分。正式规模按初步方差决定，而不是把同一视频的帧视作独立样本。
3. 除对Full的PSNR/LPIPS，还需文本一致性、运动质量、动态程度和独立偏好评估；Full只作保真参考，不是真实世界质量上限。
4. 对比Full与减步、匹配预算的成熟cache、朴素组件组合、同样搜索预算的调参/进化搜索；如加入小训练，双方匹配数据和参数量。
5. 放大投入的暂定门槛：同真实延迟下，至少一种预设主要质量指标稳定改善且无明显运动/语义退化，或匹配质量下额外降低至少20%延迟；在两个预算和第二模型复核。20%是资源门槛而非顶会录用标准。
6. 完整消融明确归因新增机制；不能将某个小网络/Gram矩阵写法作为本身的新颖性。与最近邻的方法差异和实验覆盖一起决定是否投稿。

## 来源

- TaylorSeer：https://taylorseer.github.io/TaylorSeer
- RFC：https://cvlab.yonsei.ac.kr/projects/RFC
- OUSAC：https://arxiv.org/abs/2512.14096v1
- CacheQuant：https://arxiv.org/abs/2503.01323
- ERTACache：https://github.com/bytedance/ERTACache
- EpaCache：https://arxiv.org/html/2608.29264v1
- HarmoniCa：https://icml.cc/virtual/2025/poster/45499
- DiffSparse：https://arxiv.org/html/2604.03674
- E-DiT：https://arxiv.org/html/2602.13993v1
- STG：https://ar5iv.arxiv.org/html/2411.18664
- SLA：https://github.com/thu-ml/SLA
