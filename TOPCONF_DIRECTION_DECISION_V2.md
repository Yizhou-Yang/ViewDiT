# 视频时间局部控制：第二轮方向决策

日期：2026-09-21。所有帧编号均为0-based，像素数值范围为[-1,1]。

## 结论

当前成果仍不足以作为成熟的顶会方法论文。不是缺少包装，而是两项根本证据不足：与最直接近邻相比的独立方法贡献，以及相对像素合成/普通优化的真实应用收益。

本轮明确选择继续验证的唯一研究主线是：**冻结视频VAE的时间非干扰控制，重点研究参考统计解耦与边界条件可控性**。不再把“像素约束转latent”“任意帧停止编辑”“Anderson终端反馈”作为主贡献。

通俗解释：先回答“编辑未来时，为什么已经确认的过去仍会变；怎样在不重训VAE的条件下切断这条额外依赖？”而不是承诺“时间压缩后的一个latent可以任意独立控制其中每一帧”。这改变了研究范围：当前最强正向结果解决未来改动影响过去的问题，**没有解决编辑结束后的正向拖尾，更没有解决任意块内硬切**。

这是研究资源投向的选择，不是对新颖性充分、效果全面领先或顶会接收的保证。若仅停留在固定GroupNorm统计，仍不足以构成强方法论文。

## 本轮证据概览

V100-SXM2-32GB，主要机制实验FP32，无VAE/DiT权重训练，无依赖升级。

| 实验组 | 结果条数 | 状态 |
|---|---:|---|
| 块内边界与feature sidecar | 18 | 完成 |
| 显式核心预算控制 | 24 | 完成 |
| 全坐标局部Jacobian | 2 | 完成 |
| CogVideoX依赖与合法分批 | 12 | 完成 |
| Wan自由过渡预算 | 32 | 完成 |
| Cog参考统计重放初测 | 8 | 完成 |
| Wan更充分收敛审计 | 6 | 完成 |
| Cog独立内容验证 | 16 | 完成 |
| Cog参考统计仿射编译 | 8 | 完成 |
| Cog原生latent统计投影 | 8 | 完成 |
| 总计 | 134 | 全部complete=true |

134条是配置级结果，不是134个独立视频。最初4条内容用于探索；另取bmx-trees、boat、bus、dog四条新内容验证冻结的统计重放方法，随后对这些内容做实现计时审计，不能重复称为新持出集。

## 一、哪些分支应停止

### 1. 复杂块内回溯，不作为贡献

任务：核心帧5、6保持，帧7开始恢复原VAE重建。核心相对RMSE预算1%，32次反向。

| 方法 | 逐例保护MSE降幅的均值 | 核心相对RMSE均值 | 时间均值 | 前向均值 |
|---|---:|---:|---:|---:|
| 冻结边界块，只优化未来块 | 20.425% | 0 | 31.62s | 65 |
| 全量回溯 | 1.541% | 0.894% | 76.85s | 220.875 |
| 仅边界块回溯 | 21.103% | 0.931% | 76.78s | 220.5 |

边界块回溯相比简单冻结仅多0.678个百分点，约2.43倍时间。全量回溯会被边界预算卡住，是一个弱优化器；不能利用它衬托新方法。以MSE均值之比汇总，冻结和块回溯降幅分别18.016%、18.866%，不能混用两种口径。

核心1%是平均相对RMSE，不是最大像素误差1%；最大像素差仍可能约0.06。Adam状态未随候选回退而回退，保护损失也未保证单调。结论只针对这些实现，不证明全局不可达。

### 2. Feature sidecar，不扩大投入

固定熊视频几何/颜色两个pilot，rank8随机或二阶矩主方向feature残差，只在边界之后注入。核心保持为0，但主方向sidecar保护MSE降幅38.34%/56.26%，低于同量级控制参数的像素残差46.79%/73.85%。像素硬拼接则按构造精确达到目标。

该sidecar已不是原生latent；参数比较未计入basis和scale额外存储，主方向计算未中心化，不能严格称中心化PCA。没有理由仅靠继续增大参数把这个分支包装为贡献。

### 3. 通用原生latent统计投影，没有优于普通像素优化

新增原生投影pilot：Cog、熊/黑天鹅、64×96、最后latent几何编辑、32次反向。分别优化原生latent的GroupNorm统计匹配和直接保护像素误差，标准decoder最终验证。

| 方法与编辑权重 | 前缀MSE降幅均值 | 编辑相对RMSE均值 |
|---|---:|---:|
| 统计匹配，权重1 | 90.033% | 31.353% |
| 像素约束，权重1 | 91.757% | 27.835% |
| 统计匹配，权重10 | 72.591% | 11.410% |
| 像素约束，权重10 | 80.318% | 10.603% |

在这4个配对配置中，像素约束在前缀MSE和编辑RMSE均更好；最大像素指标并非全部更好。统计投影没有展示独有收益，不能把重放的成功迁移成“标准decoder原生latent修复已解决”。

## 二、边界控制的真正限制

### 局部谱证据

两条视频resize32×48，固定历史状态，边界latent块16×4×6，共384个变量，逐坐标中心有限差分h=0.01，无随机子空间替代。

即使只观察该块第一帧，两例在相对阈值1e-3与1e-4均为384/384数值秩。锁定前两帧时：

| 内容 | 最小奇异值 | 最大奇异值 | 条件数 |
|---|---:|---:|---:|
| bear | 0.5823 | 39.9114 | 68.54 |
| blackswan | 0.5018 | 28.1343 | 56.06 |

最弱方向h与h/2有限差分差异约0.86%/0.62%；幅度0.01的真实非线性扰动与线性预测相对差约1.41%/1.25%。很小扰动有浮点噪声，较大扰动有非线性余项。

条件命题：若固定历史状态下，核心输出对边界latent的真实Jacobian满列秩且连续可微，则逆函数定理给出局部单射；局部精确保持核心可能固定该latent，从而失去改变同块其它帧的自由度。

这不是对高分辨率全空间、远处latent、改动更早latent、其它模型或额外表示的全局不可达证明。有限差分满秩本身也不是严格解析证明。该命题是标准数学工具的应用，不能把逆函数定理或使用Jacobian当新颖贡献。

### 允许过渡能否得到可用解

冻结latent0至2，故帧0至8原样保持；核心结束帧7，保护区起点7/9/11/13。24步普通Adam，桥接区不提供目标。阈值固定为保护RMSE不超过核心编辑RMS的10%。

| 自由过渡帧数 | 达标数 | 保护相对RMSE均值 | 重编码淡化的核心相对RMSE均值 |
|---|---:|---:|---:|
| 0 | 0/8 | 62.72% | 77.67% |
| 2 | 0/8 | 25.27% | 54.32% |
| 4 | 1/8 | 19.32% | 46.99% |
| 6 | 2/8 | 17.35% | 44.64% |

明确正向：可以精确锁定核心并减少拖尾；明确负向：绝大部分未达到预设预算。这里只能报告最少已测试过渡范围，不是全局最小过渡。

收敛审计固定熊的两类困难样例、六帧过渡。96步Adam冷启动、96步Adam重编码热启动、最多80步L-BFGS重编码热启动，最终保护相对RMSE为：

| 内容 | Adam冷启动 | Adam热启动 | L-BFGS热启动 |
|---|---:|---:|---:|
| 熊几何 | 11.12% | 11.10% | 11.35% |
| 熊颜色 | 12.95% | 13.28% | 13.60% |

都未达到10%预算。更多迭代确有改善，因此不能把24步结果当误差极限；但这次更充分的优化仍没有可稳定达标的主方法。

像素淡化本身精确满足核心和保护目标。任何latent方法都必须用再编辑、存储/时延或真实感知质量证明额外价值，不能人为排除像素对照。

## 三、唯一保留的正向干预

### 完整decoder而非卷积标签决定因果性

CogVideoX diffusers0.31.0默认latent_batch=2，17帧对应5个latent。未来块会通过5D GroupNorm共同均值/方差改变同一次执行中的较早输出。最后latent影响帧9至12，但在默认分批下不影响帧0至8。

合法latent_batch=4或5都把这5个latent放进同一批，二者实际上是相同执行分组，不是两个独立配置证据。相对默认分组，零编辑输出也会变化。因此分组配置必须冻结，不能随意改分组再声称质量免费提高。latent_batch=1不属于有效对照，因为首帧特殊上采样会导致输出帧数不符。

这类GroupNorm时间耦合从数学和已有因果VAE设计上并不新颖，不能声称首次发现。独立价值若成立，应是对冻结生成管线的可检验非干扰保证、保持参考重建的干预、以及真实应用收益。

### 方法：参考统计解耦

先对参考latent做一次原始decode，记录每个GroupNorm及每次调用的均值mu_ref、逆标准差rs_ref。编辑decode时不重新使用当前视频的跨帧统计，而使用固定参考统计。原始卷积、时序展开和条件分支保持不变。

可编译形式为逐通道仿射：

    a = gamma * rs_ref
    b = beta - mu_ref * a
    y = a * x + b

代数上，这是替换统计依赖路径，不是全feature恢复，也不是像素结果拼接。它保留神经网络对编辑输入的响应，但改变decoder执行，需要参考状态和匹配的分组/形状。不能称普通原生latent兼容。

若其它运算对被保护输出已满足因果性，固定统计可移除该统计通路造成的反向依赖。因果保证仍应逐架构验证，不能直接推广到未来注意力或非因果卷积模型。

### 初测与独立内容验证

旧4条视频、64×96、最后latent、0.1/0.4几何幅度，8/8前缀变化相对重放零编辑为0，编辑幅度约99.82%至101.36%。

随后冻结方法与判据，使用未参与此前探索的bmx-trees、boat、bus、dog，直接读取原始JPEG，224×128、17帧。分别编辑latent2与latent4，几何幅度0.4及encode后的颜色矩形，两边界×两编辑×四内容，16/16通过预设机制判据。

| 指标 | 独立内容结果 |
|---|---:|
| 标准decode的保护前缀最大变化 | 0.01406至0.20678 |
| 重放decode前缀变化，相对自身零编辑 | 全部0 |
| 重放零编辑相对标准decode最大差 | 不超过2.99e-6 |
| 编辑块相对标准编辑的RMSE | 1.13%至5.66%，均值3.18% |
| 编辑幅度比例 | 99.60%至101.03% |

编辑幅度保留不等于语义保留；几何扰动本身会造成明显形变，颜色矩形也不是文本语义编辑。前缀原生泄漏在正常尺寸下有些很轻微，放大20倍误差图不能用来冒充肉眼巨大提升。

直接改为逐帧GroupNorm也去除了该反向依赖，但会改变零编辑重建。参考统计重放保持参考输出到FP32舍入量级，这是与直接替换归一化相比的实测区别。

### 工程改进与资源开销

编译逐通道仿射后，对4条新内容×2个边界重新验证，8/8前缀变化为0，零编辑最大差不超过3.79e-6，与hook重放最大差不超过4.79e-6。

在同一组受控测试中，热身后3次同步decode取中位数，再对8配置平均：

| 执行 | 平均decode时延 |
|---|---:|
| 原始标准decoder | 0.8079s |
| hook统计重放 | 0.8469s |
| 编译仿射重放 | 0.7857s |

另需参考统计捕获0.8227s、编译0.01185s，均不含encode与模型加载。单次使用总成本更高；复用参考统计后每次decode才接近原生。约2.7%的decode差异没有重复实验置信区间，不能宣称显著加速；可信结论是没有大的逐次decode开销。

独立内容初测的单次hook计时约1.238s，与后续热身、重复同步计时口径不一致；不能用1.238到0.786宣称方法加速比。后续受控结果应优先使用。

统计存储4736个FP32标量，18.5KiB，224×128时约为latent标量数13.2%。运行期展开仿射系数47104个FP32标量，184KiB，不能把18.5KiB误称全部运行内存。模块名、调用序号、shape、batch配置也必须匹配；这些是实现约束，不是通用压缩格式。

## 四、新颖性与竞品的真实边界

| 工作 | 已有覆盖 | 我们可研究但尚未证明充分的差异 |
|---|---|---|
| PELC / DecFormer，CVPR2026 | 像素等价latent合成、逐通道混合与残差、Jacobian局部分析、完整FLUX应用 | 硬锁定时间输出下的条件可控性；执行状态路径的非干扰。不能仅称视频版PELC |
| LFV | 跨VAE响应校准、有效性筛选、往返漂移、频域算子库及速度 | 单个非平移不变时间边界、固定核心和执行状态；不能仅称可行性筛选 |
| IV-VAE，CVPR2025 | 时间压缩块内不均衡、组因果卷积、改架构训练 | 冻结已训练VAE的非干扰干预，而不是首次发现块内不均衡 |
| EasyV2V / VideoCanvas | 时间与空间编辑控制或精确时间条件 | 对解码阶段的像素保持条件和约束失败机制，不是首次时间mask |
| RefDecoder / 条件VAE解码 | 参考特征或多尺度decoder条件注入 | 仅用参考统计、明确非干扰验证，而不是泛称decoder参考注入 |
| 像素拼接 / 淡化 | 最终渲染可精确保护并保留编辑 | 我们尚未证明整体应用优势，这是最关键未过关基线 |

PELC是上一轮遗漏的最直接强近邻。上一轮“解码器感知像素约束到latent”的宽泛新颖性定位需要撤回，而不是继续沿用。

本轮没有找到完全相同的“视频VAE参考统计非干扰”论文，但有限检索未命中不能证明首次提出。固定参考统计、AdaIN、参考风格控制等已有技术也使单一模块创新较弱。

## 五、后续唯一主线与停止标准

研究问题固定为：在不重训生成主干时，能否用参考状态解耦实现可复用的时间非干扰，并为做不到的块内边界提供可靠的条件诊断？

目前已有可运行路径是参考统计捕获、仿射编译、零编辑校验、未来编辑解码。它是明确的正向干预，不是继续调求解器。条件谱与过渡前沿作为解释和适用边界，不能替代应用贡献。

下一阶段不再扩展失败的sidecar/回溯/原生统计投影。优先做下面三个有决定性的验证：

1. 接入真实语义编辑器及连续两轮编辑。比较标准解码、像素拼接、拼接后encode、参考重放；统一总时延、语义遵循、已确认片段变化与感知质量。必须证明至少一个像素拼接不能同等便宜完成的实用收益。
2. 补第三种具备不同归一化/分块机制的VAE，直接读源码并测试完整计算图。Wan作为严格块因果对照，不能假装它也需要Cog式归一化修复。
3. 在结果前固定不少于30条独立内容与多种编辑边界，以视频为统计单位做配对区间和盲评。当前4条新视频远远不够支撑普适显著性。

继续条件：真实多轮编辑中，在固定保护误差和相近编辑质量下，相比像素拼接/重编码出现稳定可解释的收益，且不依赖更高算力或改变评价目标。

停止条件：如果真实应用只需渲染像素，拼接同样或更好；或者参考统计明显抑制语义变化；或者贡献只能总结为已知归一化事实和一个固定统计技巧，则停止将此路线作为顶会方法论文。可保留为可复现机制分析/工程成果，但不能承诺投稿档次。

## 视觉核查

已检查公交车几何编辑、船颜色编辑、熊颜色过渡关键帧。统计重放保留编辑块主要变化，保护前缀的放大误差消失；这不等于编辑本身自然。熊的自由过渡仍留残差，重编码会改变核心颜色；像素淡化的保护区域按构造精确。

提供四组关键帧与GIF，包含成功和失败样例。GIF仅供浏览，指标来自FP32张量，不从压缩动图计算。

## 复现与审计

核心源码、原始stats、汇总、数据清单与可视化随包提供，不含模型权重与大张量。源输入PT及完整Jacobian保留在研究机结果目录。数据来源与权重SHA256沿用已核验官方记录。复现入口见BOUNDARY_REPRODUCE.md。

工具过程中曾有SSH上传失败，重查本机/远端进程和文件后补传；没有将连接失败当实验失败。最终统计只汇总complete=true的组。

最终立场：已确定继续投入的较窄问题，尚未确定一个“已经足够novel、全面效果显著、可作为顶会成果”的方法。对这一差别必须保持诚实。

## 关键来源

- [PELC / DecFormer，CVPR2026官方论文](https://openaccess.thecvf.com/content/CVPR2026/html/Bradbury_Your_Latent_Mask_is_Wrong_Pixel-Equivalent_Latent_Compositing_for_Diffusion_CVPR_2026_paper.html)
- [LFV原文](https://arxiv.org/html/2608.07569)
- [IV-VAE，CVPR2025官方论文](https://openaccess.thecvf.com/content/CVPR2025/html/Wu_Improved_Video_VAE_for_Latent_Video_Diffusion_Model_CVPR_2025_paper.html)
- [EasyV2V](https://arxiv.org/html/2512.16920)
- [RefDecoder](https://arxiv.org/html/2605.15196)
- [PyTorch GroupNorm定义](https://docs.pytorch.org/docs/stable/generated/torch.nn.GroupNorm.html)
- [Wan2.1官方VAE实现](https://github.com/Wan-Video/Wan2.1/blob/main/wan/modules/vae.py)
- [CogVideoX官方权重](https://huggingface.co/THUDM/CogVideoX-2b)
- [DAVIS-Edit数据来源](https://huggingface.co/datasets/AlonzoLeeeooo/DAVIS-Edit)
