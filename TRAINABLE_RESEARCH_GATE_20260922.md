# ViewDiT：允许训练后的研究筛选与实测（2026-09-22）

## 当前决策

目标是 novel 且显著的顶会成果，免训练、冻结全部权重不是限制。旧GN参考恢复保留为已验证的限定配置组件，不再以此承担主论文贡献。

当前没有顶会级正面结果。训练资源关已通过；候选方法关尚未通过。下面的结构化交互方法只是优先验证假设，不是已证明空白，也不是录用预测。

## 1. 本轮实际实验

### CogVideoX-2B LoRA训练可行性

环境：Tesla V100-SXM2-32GB；torch 2.6.0+cu124；diffusers 0.31.0。复用本地完整CogVideoX-2b，不下载新权重、不安装PEFT、不改变基础checkpoint。

实际配置：30层全部attn1.to_q/to_v插入rank8 LoRA；60个线性层；可训练参数1,843,200；包含adapter后实际参数总数1,695,627,072。模型商品名称2B不等于本探针实际加载参数为精确20亿。冻结基座FP16，LoRA A/B FP32；非重入gradient checkpointing；AdamW lr1e-4；FP16 autocast和GradScaler。

| 配置 | 更新次数 | 热身后每步时间 | 峰值allocated | 峰值reserved | 梯度及参数更新 |
|---|---:|---:|---:|---:|---|
| 17帧，128×224 | 3 | 0.309/0.300秒 | 3.47 GiB | 3.84 GiB | 全部有限，B发生非零更新 |
| 49帧，480×720 | 3 | 16.826/16.853秒 | 7.02 GiB | 8.24 GiB | 全部有限，B发生非零更新 |

480P loss：0.192161247 → 0.192156345 → 0.192141816。仅说明这三步没有数值失败，不能用如此小的变化说明编辑能力提升。

协议边界：缓存bear源latent的自去噪，空文本，固定t=500及噪声；使用正确的v_prediction目标；batch=1；VAE和T5不驻GPU；没有源视频条件分支、mask分支、四世界联合图、推理采样或decode质量评价。小尺寸版本对已有latent做空间插值，因此仅作资源检查；480P版本不做空间插值，输入latent为[1,13,16,60,90]。

按480P热身后时间线性估算1000步约4.68小时，仅限该单分支探针。四世界若四次同成本前向反向，粗估约18.7小时/1000组，尚非实测。正式编辑器的条件分支、缓存编码、数据加载、验证与保存均需另计；不能声称完整编辑训练只需7GiB。全参数训练和H20尚未实测。

脚本：cog_lora_trainability_probe.py。
结果：cog_lora_trainability_128x224_17f_stats.json、cog_lora_trainability_480x720_49f_stats.json。

### 四世界损失冗余审计

假设所有四世界目标均已提供，残差堆为e，H为归一化4×4 Hadamard矩阵。由于H^T H=I：

||He||_F² = ||e||_F²。

所以完整、等权正交“主效应+交互效应”监督与普通四个成对MSE完全等价。只加二阶对比c=(1,-1,-1,1)，则：

||e||² + λ||c^T e||² = e^T(I+λcc^T)e。

这是联合误差度量的重新加权，不是新增目标信息；可能有优化收益，但不能自动宣称新因果可识别性。单独二阶项对共同漂移e00=e10=e01=e11完全失明。

V100 FP64实测：正交改写loss相对差1.11e-16，梯度最大差1.78e-15；共同漂移的二阶惩罚为0；附加二阶项与显式二次型相对差0。

淘汰对象仅为“把成对MSE换成完整等权差分就产生新方法”的论点。不排除结构归纳偏置、部分监督、非线性损失或采样设计真正带来收益。

脚本：counterfactual_loss_audit.py；结果：counterfactual_loss_audit_20260922.json。

## 2. 竞品约束

| 最近邻 | 已覆盖 | 我们不能再宣称 |
|---|---|---|
| SIGE / Tiled VAE | 参考归一化统计重用 | 首次统计重放 |
| PELC / RefDecoder / Mirage | latent合成修正、参考decoder、训练式和谐化 | 在decoder加可训练修正器就是新方法 |
| EditCtrl | 局部/全局控制解耦 | 通用保护与释放adapter就是空白 |
| LiveEdit / InfinityEdit / Memory-V2V | 流式编辑、未来点火、历史检索与跨轮一致性 | 轻量adapter加未来编辑或历史记忆就是新颖 |
| AlbedoEdit | 插入/删除/纹理统一，首帧albedo条件，合成paired训练，隐式光照effects | 操作条件化+物理效果+多任务训练尚无人做 |
| EffectErase / ROSE | 对象effects定位，插入/移除，soft difference监督 | 加task token、soft effects mask、互逆loss就是空白 |
| Vera / DBL-Diffusion | 显式编辑层、alpha、合成分支，effects层监督 | 只生成修改层、其它像素保留就是新范式 |
| VOID / CSVC | 物理交互反事实、因果steering | 首次因果视频编辑 |
| FactorMatte / Generative Omnimatte | 反事实分解、条件跨层交互、背景残差、solo与clean-plate | 首次发现对象图层不独立或首次交互层 |
| No Shadow Left Behind / GeoRemover | proxy差分渲染、几何引起的effects变化 | 物理代理+差分编辑就是新颖 |
| RFDM / FlowPortal / FlowEdit | 时序残差生成、源目标流差、恢复残差 | 残差/差分去噪本身就是新意 |
| Causal Writability（2026-09-14） | 物理变量预测低维激活写入与深度边界，1.3B复验 | 简单低维物理干预是新发现 |

本轮重点重新核对AlbedoEdit、Vera/VOID、FactorMatte；FactorMatte正文明确使用条件先验，并含environment/residual/foreground三层，复杂情形允许多层交错。其并非一个朴素独立加法层基线。未逐一复现官方模型，不能声称这些竞品在新协议上已经失败。

## 3. 优先验证假设：随编辑重新求值的交互效应

### 问题收窄

先不做通用物理世界模型，不同时解决长视频、因果动力学和任意指令。先考察短视频中两个对象或一个对象与接收表面的视觉交互：共享阴影、对象在另一表面上的反射、遮挡关系。操作限定存在/移除与外观属性变更；所有受控相机、轨迹固定，暂不声称删除对象后动力学反事实。

关键不是“给对象扩大一个effects mask”，而是：某个像素的必要变化可能依赖多个实体及各自操作，不能固定归给一个对象。删除A时，B依然造成的效果不能一起擦除；改变A的外观时，应更新相关反射而不盲目删除全部阴影。是否不变以受控渲染为准，不能把改色不影响阴影当普遍物理规则。

### 最小数学对象

同一场景和同一外生条件u，定义两项二值干预后的四个真值视频x00,x10,x01,x11。指标在同一颜色表示、曝光和像素坐标下计算；固定相机不意味着所有编辑都保运动，只是本阶段任务边界。

I_AB = x11 - x10 - x01 + x00。

在B未改变时A的增量为x10-x00；在B已改变时为x11-x01；两者之差正是I_AB。I_AB是有限差分交互，不自动等价于可识别的物理机制，也不是新数学。

考虑固定单项加法残差预测x_hat(a,b)=c+a*rA+b*rB。若I_AB≠0，这个受限模型不能同时精确拟合四世界。该结论不适用于一般条件扩散、alpha合成或FactorMatte；普通条件模型完全可能学会相同交互。

### 候选实现而非既成贡献

使用共享场景编码器，从源视频、对象mask及操作描述预测单项响应和条件交互响应；在编辑后的实体状态上重新求值。相对基线源状态，用单项增量及交互增量共同调制DiT残差更新，不把固定effects mask作为完整充分状态。训练时用同场景的多干预结果约束响应；推理仅给源视频、mask和指令，不提供真值交互图或未来目标。

二值原型可写为：x_hat_ab = x00 + a*rA(s) + b*rB(s) + ab*rAB(s)。这个式子只是四状态饱和参数化，单场景拟合本身没有新意。研究价值必须来自共享响应模块跨场景、跨对象对和未见组合的泛化与数据效率，而非恒等分解。若扩展为连续操作，应对操作取值进行条件求值，不假设全局线性；存在覆盖冲突和物理次序时不强制交换。

方法层必须证明：相同输入、数据、参数与计算预算下，结构化响应比普通条件LoRA更能学习未见交互，且不是单纯辅助监督、损失重加权、额外参数或额外目标造成。若做不到，该候选不够novel/significant，应停止，而不是改名。

## 4. 最小实验与停止标准

### 数据

先生成200个训练场景、40个开发场景、80个封存测试场景，每场景同对象构成四世界。按场景、资产和对象对划分，不随机切帧。开发集可调；封存测试在实现及门槛冻结后一次评估。此规模是资源评估后的初始计划，不是已经生成的数据。

用相同模拟初态、固定曝光/相机、已保存轨迹、相同渲染采样设置；无编辑重复渲染估计noise floor。必要变化区域由真值差分与noise floor共同定义；边界软区域和低信噪比单列，不能凭任意阈值制造成功。测试组合可在训练中见过基本操作，但不能见过同一资产对和相同场景的对应组合。

需要高保真渲染或可信公开四世界数据；当前机器未找到Blender，尚未安装或构建数据。不能将PIL画圆或独立贴图当作已验证光照反事实。真实外部验证至少另收30条独立视频，包含失败，不作为调参集。

### 公平对照

B0：普通多任务条件LoRA，完整源视频+同样mask和指令。
B1：B0加同样真值effects软图辅助监督，参数补齐。
B2：B0加完全相同的跨世界loss，排除损失本身优势。
B3：同架构同预算但删除结构化交互项；将参数重新分配给主分支。
B4：同四世界训练数据但打散成独立paired样本；同时报告相同样本曝光次数和相同GPU时间两个口径。
B5：适用任务上的官方EffectErase、AlbedoEdit、Generative Omnimatte等。未公开或无法部署的权重不编造结果，区分官方结果与自实现近似。
Oracle诊断：给相同生成器真值必要变化区域，只用于分析定位与生成误差，不进入正常测试排名。固定扩大mask、独立残差相加只作弱基线。

### 评价

分别报告对象编辑正确性、必要effects区域目标误差、应保持区域漂移、交互对比误差及边界/时序质量。全图PSNR可能掩盖小面积effects错误；全图亮度不是对象语义成功率；CLIP不能单独裁决物理正确性。以场景为统计单位进行配对bootstrap，不能把帧数或随机种子当独立视频数。

### 预设资源闸门，不是已观测结果

1. 数据闸门：四世界可重现，交互信号明显高于重复渲染噪声；否则先修数据而非训练。
2. 学习闸门：先用小尺寸检查B0能拟合开发任务，再进行3个训练种子的同预算比较。B0没学会时不得把候选相对差异归为novelty。
3. 扩大投入闸门：未见组合中必要effects误差较最强匹配预算基线下降至少20%，场景级95% CI排除0；对象编辑成功率下降不超过2个百分点，保持区和时序指标不劣化超过5%。近零基线误差应另定绝对容差，不能用不稳定相对比值。
4. 成果闸门：真实视频盲评偏好至少60%且场景级CI下界高于50%，同时保持与effects优势迁移。30条仅pilot，不足则扩大独立测试，不按帧夸大样本量。
5. 若优势消失于B2/B4或高阶交互普遍破坏二阶结构，则停止当前结构假设。只有拟合合成数据、mask分割更准或toy代数成立，均不算顶会结果。

这些是筛选投入的门槛，不是通用显著性定义或录用保证。最终还需规模、泛化、基线覆盖和复现完整性。

## 5. 已完成与未完成

已完成：两档真实LoRA训练资源探针；四世界损失代数/梯度审计；旧语义状态报告纠错；最近邻补充审查。

未完成：操作条件编辑训练、四世界真实数据、候选结构实现、官方编辑baseline复现、独立真实视频盲评、H20训练/推理验证。没有后台训练在继续，没有提交或推送本轮更改。

## 关键来源

- AlbedoEdit：https://vcai.mpi-inf.mpg.de/projects/AlbedoEdit
- EffectErase：https://arxiv.org/html/2603.19224v1
- Vera：https://ar5iv.labs.arxiv.org/html/2606.23610
- VOID / Vera官方介绍：https://netflixtechblog.com/toward-more-controllable-ai-video-editing-an-early-research-exploration-at-netflix-eb8160ed60a2
- FactorMatte：https://ar5iv.labs.arxiv.org/html/2211.02145
- Generative Omnimatte：http://gen-omnimatte.github.io/
- No Shadow Left Behind：https://grail.cs.washington.edu/projects/shadowremoval/
- ObjectClear limitations：https://arxiv.org/html/2505.22636v2
- DBL-Diffusion：https://arxiv.org/abs/2607.25802
- RFDM：https://arxiv.org/html/2602.06871v1
- Causal Writability：https://export.arxiv.org/abs/2609.15980
