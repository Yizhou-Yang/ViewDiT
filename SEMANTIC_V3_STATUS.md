# 第三轮真实语义验证

## 结论纠错（2026-09-22）

末节关于“冻结模型根本无法把棕熊变成北极熊”“假说A被证伪、假说B成立”的普遍判定撤回，旧文字仅作错误追溯，原始实验数值不改。上文高强度补测已记录可辨认白熊，低强度单样例不能排除底座能力。所谓“对象白度”实际是整个编辑时段的全画面平均值，未使用对象分割；亮度约0.9不是北极熊编辑的有效必要条件。

本次重新读取本地 `cog_semantic_protect_ablate.py`，确认 `none` 只取消去噪回调中的pin，解码前仍对全部模式执行 `ze[:, :, :args.start_latent] = z[:, :, :args.start_latent]`，因此不是完全无保护的端到端协议。当前源码不能单独证明所有历史运行使用的脚本版本。此消融也没有实现attention注入，不能证伪未实施的方法。用户已允许训练，免训练不是研究硬约束。

## 已完成

官方CogVideoX2b真实文本条件去噪已运行。480×720，49帧，50步总时间表，strength0.6，每轮30次有效去噪，先“北极熊”后“熊猫”。相同视频、相同未来区域、两次改写；并非多个独立内容，也不是任意多块编辑组合。

两轮组complete=true，共10条方法结果。17帧4步资源smoke也complete=true，共5条，但不用于语义质量判断。

| 轮次 | 原始decode前缀最大变化 | 旧固定统计编辑RMSE | 双参考编辑RMSE | 紧凑参考合成前缀误差 | 紧凑参考合成编辑误差 |
|---|---:|---:|---:|---:|---:|
| 第1轮 | 0.06473 | 4.623% | 0.336% | 2.68e-6 | 0 |
| 第2轮 | 0.09410 | 5.063% | 0.500% | 2.68e-6 | 0 |

两轮DiT去噪约186.17/186.29秒，不含encode、decode、CPU/GPU搬运、模型加载和往返审计。每种方法使用同一个真实DiT输出，不存在重新抽噪声优势。

## 必须保留的语义失败

已查看两轮关键帧。第一轮熊仍为棕色，没有达到“白色北极熊”目标。第二轮脸部出现熊猫样黑白变化，但身体仍为棕色，不能算完整“黑白熊猫”编辑成功。

因此，这两轮证明了真实生成latent上的参考保护和恢复机制，不证明底座语义编辑质量，更不证明方法提升语义遵循。所有结果和失败图都保留。

## 真正保存与加载

真实生成latent上，存下参考统计后重新加载，仅解码前5个latent恢复13帧保护前缀，再与完整49帧原始编辑输出合成。旧latent基线也仅保留第4个latent并进行同样前缀解码，不强制保存整段旧未来。

两轮紧凑参考恢复最大误差2.68e-6，编辑区域最大误差0。恢复阶段约9.92/9.95秒；旧FP32/FP16 latent基线约9.64至9.65秒。没有加速优势，存储是主要差异。

FP32统计张量为9472字节。直接torch分散序列化含32538字节开销，已开发VCAP0001格式，将布局、模型版本、分辨率、边界与统计一起保存，实际文件9450字节；加载后所有统计张量逐元素相同，内部SHA256验证通过。

对应旧单块latent FP32张量345600字节、torch文件346780字节；FP16张量172800字节、文件173980字节。在这条熊内容上FP16恢复误差8.14e-6，已经满足1e-5预算，因此不能声称只有本方法达标；本方法提供更小状态而非唯一可行解。

旧smoke与49帧生成组中的capsule_compositor额外解码时间记录0是初版漏计，不是免费。可靠成本使用独立恢复实验约9.9秒。修正版脚本已补计，原始结果不覆盖。

## 固定补测

在第一轮明确未满足目标后，固定一次strength0.85、同prompt、同49帧、50步总表，即42次有效去噪的补测。不重做roundtrip审计，缺项为null，不填0。该补测状态和视觉结果待完成后写入下节；未完成时不得声称语义成功。

## 高强度补测完成

strength0.85、42次有效去噪、480×720、49帧已完成，5条方法结果，complete=true。关键帧可辨认白色北极熊，说明底座在这个开发样例上能达到粗粒度物种/外观目标；身体位置、姿态和编辑区背景也变化，未通过精准对象编辑或自然过渡盲评。该成功不抹去之前两轮不完整结果，也不能用于报告泛化成功率。

原始decode保护前缀最大变化0.1333814；全局参考与双参考前缀最大差均4.81e-6。编辑RMSE全局参考2.821%，双参考0.399%。紧凑参考合成最大保护误差2.68e-6，编辑区逐元素等于原始生成结果。其本次完整49帧额外decode实测28.72秒，未使用后续前缀裁剪；不能称免费。

已完成的前缀裁剪恢复实验在strength0.6两轮上约9.9秒，并验证VCAP文件精确往返；尚未对strength0.85另跑序列化裁剪恢复时延，不能混写为这次补测实测值。

本轮所有11个主要实验组共242条配置结果完成，另有2条VCAP序列化往返验证。配置数不是独立视频数。

## 去噪阶段保护强度消融（协议第①点·决定性）

为回答"棕熊→北极熊语义失败究竟是由保护区 pin 压制，还是冻结模型本身缺乏此能力"，在**去噪（DiT latent）阶段**对前缀保护强度做了三档消融：`none`(完全不 pin 前缀)、`half`、`full`(默认全 pin)。对象指标用编辑区**白度**：(亮度=RGB均值归一化，色度=mean(|RG|+|GB|+|BR|)/3)。北极熊应亮度≈0.9+、色度≈0。

| 保护档位 | 前缀max漂移 | 编辑RMSE | 编辑区亮度 | 亮度增益 | 色度下降 |
|---|---:|---:|---:|---:|---:|
| none | 0.0952 | 0.2518 | 0.355 | +0.0118 | -0.0015 |
| half | 0.0928 | 0.2176 | 0.352 | +0.0088 | -0.0010 |
| full | 0.0929 | 0.1679 | 0.347 | +0.0035 | -0.0000 |

17帧、50步、strength0.6、480×720，单轮30次有效去噪，每档complete=true。基线原始编辑区亮度≈0.344。

**结论（假说A被证伪，假说B成立）：** 即使**完全不保护**（none，前缀帧全部自由被 prompt 引导），编辑区亮度增益仍仅约 +0.012、色度不降——冻结 CogVideoX 在"白熊"prompt 下**根本无法把棕熊变成北极熊**。保护(pin)不是语义失败的原因；**冻结模型本身在该编辑任务上缺乏语义达成能力**。这同时解释了为何三档 `protected_max` 都≈0.09 而非0：DiT latent 层 pin 前缀时，解码侧 GroupNorm 统计仍受编辑区时间传播影响，无法达到解码侧专门机制（双参考）的~5e-6水平——但这对协议判定不关键，因为语义在完全自由下也未达成。

**对研究方向的含义：** 普通推理（即便无约束）无法完成目标语义编辑，说明"保护 vs 释放"必须搭配**新的生成引导机制**才能推进目标语义——纯解码/统计层面无法让冻结模型做到"棕熊→北极熊"。这支持转向"生成过程中的保护与释放 + 冲突感知引导"，并把解码侧机制降为辅助组件。
