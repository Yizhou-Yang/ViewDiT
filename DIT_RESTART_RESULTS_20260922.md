# DiT 生成质量与加速：V100 首轮组合诊断

日期：2026-09-22

## 研究决策

允许组合成熟组件，不要求各组件独立原创。论文需要的是有意义且不显然的整体贡献、相对最近邻的实质区别，以及公平预算下可重复的收益。组件数量和统计显著性均不能单独保证顶会贡献或录用。

继续探索 DiT 纯噪声生成中的计算预算分配与小训练协同，不回到视频编辑、历史记忆或 decoder 修补。本轮建立了可运行底座与初步基线，没有获得已确认的新方法或顶会级显著收益。

## 实验设置与完整性

- GPU：Tesla V100-SXM2-32GB。
- 模型：CogVideoX-2b；FP16 DiT、FP32 文本编码与 VAE。
- 生成：纯噪声 T2V，17 帧、480×720、DDIM 30 步、CFG 6、eta 0。
- 样本：一个天鹅校准 prompt 和一个公交车开发迁移 prompt，各一个 seed；不是正式测试集。
- 共完成 24 个主配置、4 个一阶外推配置，以及 10 次独立解码。三个运行记录均 complete=true，所有输出 finite=true。
- 主配置去噪合计 914.12 秒，外推配置去噪合计约 137.81 秒；总去噪约 17.53 分钟，不包括模型加载、编码、解码、I/O 和此前实验。
- 完整 30 步共 900 次 block 调用，选两组缓存 768 次，26 步为 780 次。一次 block 调用包含 CFG batch，不是一次完整 DiT NFE。
- 所有配置复用各自 prompt 的同一初始噪声；缓存状态不跨配置。

## 结果

PSNR 为解码后 RGB 相对完整 30 步输出的保真度，不是绝对生成质量。时间只含去噪，均为单次测量。

| 配置 | 天鹅时间/s | 公交车时间/s | 天鹅 PSNR/dB | 公交车 PSNR/dB |
|---|---:|---:|---:|---:|
| 完整 30 步 | 42.12 | 42.48 | 参照 | 参照 |
| 完整计算 26 步 | 36.79 | 36.83 | 20.98 | 15.90 |
| 选两组零阶残差缓存 | 36.64 | 36.73 | 27.28 | 19.07 |
| 同两组一阶残差外推 | 37.46 | 37.22 | 25.90 | 16.54 |
| 四组一阶残差外推 | 31.58 | 31.56 | 25.85 | 16.17 |

选两组零阶缓存约 1.15× 去噪加速，时间降低约 13%；加上各自独立解码时间约 1.12×。这不是完整端到端加速，且不能从单次计时推断稳定收益。

去噪峰值 allocated 显存：完整约 3.89 GiB，两组零阶缓存约 4.49 GiB，同组一阶外推约 5.09 GiB，四组一阶外推约 6.30 GiB。独立 FP32 VAE 解码约 24.0 GiB。缓存并非零显存开销。

当前零阶选组在两个样例上，比相近去噪时间的 26 步和当前同组一阶外推更接近完整输出。该外推只是自实现的一阶 block-residual baseline，不是 TaylorSeer 官方复现；不能宣称击败 TaylorSeer 或 SOTA。

## 组合机制诊断

对单组近似引入的终点位移 d_i，比较独立打分 G_ii+G_jj 与含交叉项打分 G_ii+G_jj+2G_ij，其中 G_ij=<d_i,d_j>/D。

两种打分仅使用天鹅校准样例，均选择组 [2,3]。新增交叉项没有改变实际选择，因此没有新增部署收益。公交车所有 pair 的 oracle 也为 [2,3]，但它仅用于诊断，不能用于后续封存测试选择。

校准 Gram 的所有非对角项为正，没有观察到线性位移负相关抵消。真实组合位移相对单项线性和的误差约 0.90–1.26；组 [2,3] 在两个样例上分别为 0.956 和 1.234。因此不能把线性叠加当作准确的组合预测。

这不证明所有联合策略无效，也不证明组合天然优于单项。Gram 展开本身是已知数学，不构成原创贡献。

## 固定帧视觉核验

已查看全部五种配置各自的第 0、8、16 帧，不进行事后挑帧。

天鹅：主体与池塘均可辨识；不同方案在颈部姿态、位置、水纹及倒影上有差异。缓存较接近完整输出，但静态帧不能证明视频无闪烁或运动更自然。

公交车：全部方案包括完整采样都呈明显卡通/插画风格，与提示词 realistic footage 不一致。零阶缓存较保留完整输出的车体样式；26 步和外推改变了车头、轮毂及建筑细节。因此完整采样不是正确质量标签，更高 PSNR 可能只是保留了它的风格偏差。

本轮未完成动态视频偏好评估、文本一致性定量指标、VBench 或足量统计测试，不声称绝对质量提升。

## 最近邻边界

成熟组件与相关方向包括：

- FasterCache：视频扩散缓存与 CFG 相关加速，简单缓存组合不是空白。
- TaylorSeer：高阶近似预测，外推本身不是新机制。
- CacheQuant：量化与缓存联合优化，不能泛称过去都只独立考虑组件。
- HarmoniCa：针对缓存的训练与误差优化，小训练修复缓存也不是空白。

前阶段查阅的其他联合调度、轨迹校准与学习式缓存工作同样需要逐项排除。本文没有完成官方实现复现或系统性 novelty 审计，不能提前宣称足够区分。

## 下一轮正面路线与判定

工作假设：在真实近似 rollout 上，联合选择计算动作并训练轻量修正，可能比组件各自优化后直接叠加有更好的质量-时延曲线。它是待验证假设，不是已成立的新方法。

先限制为两个执行器：残差复用与提前确定的块级计算缩减。不要立即叠加量化、稀疏 attention、CFG 和 guidance，以免无法定位收益来源和硬件开销。

必须比较：单组件；同预算的朴素组合；同样数据与参数量的普通蒸馏/adapter；近似 rollout 上的组合适配。若再加入联合策略，应与同预算随机搜索或坐标搜索比较，并计入搜索、教师与训练成本。

在真正联合部署轨迹上收集输入，不只用完整教师轨迹上的局部误差训练。是否存在值得建模的特定跨层/跨步状态，以及是否超出已有训练式缓存，需先用实证与原文确认，不能把通用训练流程换名当创新。

扩展到至少 16 个独立 prompt 起步、多个 seed、至少两个计算预算；训练、校准、开发和封存测试分开。当前公交车样例已分析，只能继续作为开发样例。

核心评估同时报告实际时延、文本一致性、主体与运动质量、动态程度和盲评；paired PSNR 只作辅助。采用 prompt 级配对统计，不能把帧当独立样本。之后才扩展到 49 帧与第二个 DiT。

阶段投资门槛可设为：相对最强公平组合，同质量下额外降低约 20% 实测时延，或同延迟获得稳定的质量收益。该门槛是项目管理标准，不是统计显著性定义或顶会录用保证。

当前停止把简单 Gram 打分作为候选主贡献；保留可运行的缓存执行器和公平采样框架。尚未启动新训练，尚未进行第二模型实验，也未执行本轮 commit/push。

## 本地证据

- dit_compute_allocation_stats.json
- dit_compute_allocation_analysis.json
- dit_forecast_baseline_stats.json
- decoded_stats.json
- cal_water_comparison.png
- test_street_comparison.png
- dit_compute_allocation_pilot.py
- dit_forecast_baseline.py
- dit_compute_decode.py

远端结果目录：/root/viewdit/results/video/dit_compute_allocation_pilot_20260922。
