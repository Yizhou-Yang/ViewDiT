# 时间局部视频编辑：研究路线与决策门槛

更新：2026-09-21。状态：V100控制原型，不是已完成的语义编辑论文。

## 研究目标

保留一段已经生成且用户满意的视频，仅在指定时间区间改变指定内容。目标不是从零生成多个事件，也不是cache加速。候选题目：Endpoint-Calibrated Local Video Editing with Protected Context（终端校准与上下文保护的局部视频编辑）。该名称只是研究描述，不代表已有新颖性证明。

当前Latte FFS原型只有固定空间几何差分方向，不能宣称已实现按文本微笑、换物体或动作修改。

## 当前可运行原型

固定所有DDPM步间随机数，保存原视频去噪轨迹。先沿固定空间方向生成一条全局原生变化，按用户时间曲线构造局部目标。在每次尾段采样中逐步回填范围外原始状态。控制器以完整75步尾段的实际终端误差更新检查点扰动。

记原轨迹为x_s^base，编辑扰动为delta，包含回填的完整尾段为G_s^P，目标为y*，编辑区投影为M_E。

- y_k = G_s^P(x_s^base + delta_k; xi)。
- r_k = M_E(y* - y_k)。
- 基础更新：delta_candidate = M_E(delta_k + eta r_k)。
- 验证更新：最近最多3次已接受的(delta,r)构成小型Anderson组合，最小化残差组合范数，系数和为1。
- 稳定性约束：岭系数0.001；组合系数L1范数不超过4，否则回退基础更新。
- 接受条件：候选终端误差严格下降且输出有限；否则保留当前解、eta减半至最低0.05并清空旧组合历史。
- 预算：固定8次尾段尝试。拒绝的尝试也计费。初始eta=0.8。

恒等逆响应、Anderson加速、接受/拒绝以及回填都不是新算法。当前实现不提供全局收敛证明；误差单调不升由接受规则保证，不是模型特殊性质。改进幅度才是需要测量的内容。

## 已完成的机制判断

第一版任意latent目标常常不可达。第二版模型原生目标改善了可达性，但完整16列响应矩阵与软泄漏惩罚仍削弱编辑，因此不作为主线。

第三版固定8次终端反馈在40至43四个种子上，相对硬回填基线平均目标误差从0.4903降至0.4230，平均像素目标MSE从0.0002886降至0.0002012；其中seed43变差。它支持终端信号有用，但反驳固定步长无保护迭代足够稳定。

第四版独立验证使用50至53四个新种子，两种此前未用时间曲线：宽渐变和双脉冲。硬回填、固定反馈和有保护反馈在同一视频上成对对比，两个反馈方法额外NFE均为600。最终完整数字以endpoint_validate_stats.json和研究报告为准。

## 新颖性定位

- TempoControl：优化cross-attention的相关性、幅度和熵来控制生成概念的时间。不能将“免训练时间曲线控制”作为我们的独有贡献。
- Prompt Relay：按时间段路由不同文本条件，并软化边界注意力。不能将“避免时间串扰”本身作为独有贡献。
- LOCO：单步posterior mean predictor的Jacobian低秩子空间与零空间局部编辑。不能将“低秩/Jacobian/局部性”作为独有贡献。
- Frame Guidance：帧条件引导与潜变量优化。当前single_step只是自有消融，未复现该论文。
- 硬回填/inpainting与Vera等合成编辑：保留范围外内容和合成本身并不新。

拟争取的区别是：在已有视频局部修改场景中，面向最终输出而非attention或单步pred-x0校准编辑，并把未指定过渡区作为生成自由度。潜在贡献是可达性与跨步偏差的刻画、约束与校准的联合设计及真正非平凡的编辑收益，而非给标准求解器换名字。

## 必须解决的平凡目标问题

当前y*=base+envelope*(global_edit-base)是完整合成目标。直接返回这个latent后解码即可零目标误差，且其生成成本更低。因此现在的控制指标不是超越简单合成的证据。

下一阶段不能继续仅拟合这类完整目标。任务改为“核心编辑区E / 自由过渡区B / 保护区P”：

1. E内部使用原生编辑参考或语义约束；P保持原内容。
2. B不提供混合目标，由生成模型处理状态切换、遮挡和非刚体形变。
3. E的命中与B的质量分开评价，不能通过消除编辑来降低边界指标。
4. 硬切、像素交叉淡化、latent混合、硬回填、同预算常规引导必须全部保留为基线。

这是未实现的研究升级，而非当前原型已有功能。

## V100上可执行的后续路径

首选接入Wan2.1/VACE-1.3B的原生编辑条件，而不是训练新主干。官方提供1.3B权重与CPU offload路径，但本环境尚未部署成功；V100的SM70不等同于官方现代GPU运行条件。必须先在独立环境检查FP16数值、attention回退、VAE精度、CPU内存与17/33帧短片的峰值显存和时延，不能直接承诺兼容。

环境已知：V100 32GB，PyTorch 2.6.0+cu124，diffusers 0.31.0。新Wan流水线可能需要独立升级依赖，不应覆盖当前Latte环境。V100无原生BF16 Tensor Core；软件API可报告支持并不说明硬件有原生吞吐。

## 继续投入与论文门槛

以下是本轮探索过程中提出的工程决策门槛，不是预注册统计检验或通用学术标准；25%控制门槛是在已看到部分验证结果后提出，不能用于宣称独立确认。未来语义实验应在运行前冻结协议：

1. 控制原型：新种子平均目标误差相对硬回填下降至少25%，逐样本失败率透明报告，预算与实际开销完整统计。
2. 最小语义验证：12个固定案例，覆盖外观切换、非刚体变化、遮挡交互；每例同时运行强合成基线。在编辑命中相当时，过渡质量不能仅靠代理指标声称改善。
3. 正式评估：至少30个独立内容案例、3个种子、3种时间曲线；置信区间按内容案例聚合，避免将相关帧当独立样本。
4. 比较：实现最相关竞品或在相同骨干上给出忠实适配，并清楚标注适配和官方实现的区别。额外提供等NFE、等时延、固定编辑命中的Pareto比较。
5. 发表主张：若无法优于简单合成的真实过渡质量，则保留为有用求解器/控制诊断，不将其包装成新的语义编辑方法论文。

研究决策：保留终端校准作为明确的推进候选，停止扩展软响应矩阵与固定滤波扫描；投稿题目与“足够novel/显著”的断言必须等非平凡语义验证通过。

## 主要来源

- TempoControl：https://arxiv.org/html/2510.02226v3
- Prompt Relay：https://arxiv.org/html/2604.10030v1
- LOCO：https://arxiv.org/html/2409.02374
- Frame Guidance：https://arxiv.org/html/2506.07177v2
- Wan2.1官方仓库：https://github.com/Wan-Video/Wan2.1
