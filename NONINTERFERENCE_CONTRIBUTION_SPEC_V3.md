# 时间非干扰：贡献规格与可证边界

## 当前可主张的内容

方法由全局参考统计重放升级为按边界选择参考仿射与编辑仿射的传输。具体解决固定参考统计与强编辑分布之间的冲突，并明确其开销、执行依赖和像素拼接对照。是否足以成为顶会贡献仍取决于真实语义结果和应用优势。

最小定义：固定参考latent z_r、参考执行配置q，编辑latent z_e在保护前缀相关latent上与z_r相同。受控decoder写为D(z_e;S_r,S_e,m,q)，其中S为层/调用级归一化统计，m为与架构时间分辨率匹配的保护标识。

## 条件非干扰命题

设计算图的所有跨时间依赖仅来自已知因果运算和对整段做统计的归一化。将影响保护输出的归一化位置全部替换为与编辑变量无关的参考仿射；同时固定其所有其它祖先输入，则保护输出对允许的未来编辑变量不变。

证明方式是沿计算图拓扑归纳，而不是仅凭“卷积是causal”推断：
1. 受保护的输入祖先固定。
2. 因果节点的保护输出只依赖已固定的保护祖先。
3. 原本连接未来到保护区的统计边被常量参考统计替代。
4. 因此每个保护中间节点及最终保护像素不随允许编辑变化。

数学上的精确不变针对同一受控零编辑执行。不同FP32实现与标准原生decode之间允许舍入差；应分别报告max误差，不能混淆。

条件不满足的例子：非因果注意力、未来条件注入、未覆盖归一化、未冻结历史latent、错误分块映射。对这些情况不提供保证。

## 双参考与单次解码的区别

双参考版本用原始edited decode的统计S_e，只在非保护区使用。这样保护区不再污染编辑区的目标归一化，但需要一次edited decode采集统计。后续层的卷积上下文已改变，因此不保证编辑像素与原始edited decode完全相同。

单次版本在保护区用S_r，非保护区直接使用当前forward的原始GroupNorm输出。它不需edited统计预扫描，但编辑区统计基于已经改变的上游特征；误差与双参考不同，应独立测量，不能将双参考精度和单次成本拼成一个不存在的方法。

## 像素拼接的强反论证

定义C(z)=M*D(z_r)+(1-M)*D(z)。如后续编辑器G只接收latent与文本、不接收呈现像素及decoder状态，那么将相同z送给G时：

    G(z,c,noise)在像素拼接和受控decode两种显示策略下完全相同。

因此“无需重编码、可以再次编辑”不是decoder侧方法的独有贡献。一个公平的像素合成系统完全可以保存z及参考像素，不重新encode。若强制基线丢弃latent再encode，得到的优势无效。

只要最终指标是精确匹配硬拼接目标，C按构造已经达到最优。受控decoder必须通过不同且真实的目标证明价值，例如避免硬切产生的感知问题、限制模型内梯度的干扰、匹配有明确部署需求的状态大小/延时。这些方向目前是待验证假设，不能列作已完成贡献。

## 顶会规格而非包装

贡献需要同时成立：
- 一个此前未被充分解决的任务约束或机制，准确覆盖PELC、LFV、LiveEdit、Mirage等近邻。
- 非平凡方法改进，能在强基线面前改变保护/编辑/质量/成本的Pareto前沿，而不只改善旧弱版。
- 可复用的验证方式或条件保证，以及跨架构真实实验。
- 足够独立数据、完整失败、预定预算、视频级统计和感知评价。

当前可检验正向点是强编辑情况下对全局参考统计的明显改进；当前最大欠缺是像素拼接强基线下的实用收益及独立语义质量。

## 资源与实验顺序

不重训大型DiT，不安装FlashAttention，不修改现有diffusers。先复用V100上的CogVideoX2b，原生空间尺寸与49帧。文本embedding单独计算、卸载T5后再载DiT，VAE全程FP32。模型下载、编码、生成、参考/编辑统计采集和最终decode分开记账。

先完成八新内容33帧冻结方法验证，再完成真实语义链路。若真实底座无法满足编辑目标，应报告底座失败而非选择性展示。若资源或网络阻塞，应报告进行中和已有成果，不冒充已运行。

## 新近邻核对

LiveEdit以三阶段蒸馏将双向编辑底座转为因果流式编辑，配合mask缓存；因此“流式、因果、保持未编辑内容”不能作为我们的首创主张。它与冻结decoder统计干预不同，但差异本身不是优势证据。

Mirage使用2D特征向3D VAE后期层注入，配合重建和harmonization LoRA，关注资产插入与空间细节；因此“解码器内参考信息、保持因果结构”也已有直接先例。我们的未训练统计传输更轻，但任务更窄，不能用参数少替代质量比较。

PELC、LFV已覆盖像素等价、响应分析与有效性筛选，继续按第二轮报告界定边界。本轮未运行这些方法的官方代码，所有对比为任务/实现范围分析而非实测榜单。
