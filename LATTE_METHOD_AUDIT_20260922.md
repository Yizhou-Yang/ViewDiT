# Latte 方法正确性自审计（Method Self-Audit）

日期：2026-09-22
底座：Latte-XL/2（UCF101，class-conditional），V100，CFG=7.0
场景：把"紧凑边界状态保持前缀 + 未来由强差异类别重写"作为强语义编辑代理，检验方法本身是否成立。

## 目的

在推进对比与迭代之前，先验证当前"紧凑边界状态保持"机制**本身没问题**——
区分三种可能真相：真机制 / 自洽回路（把算好的答案贴回去）/ 平凡线性代数（低秩投影自带的单调逼近）。

## 核心脚本

- `scripts/latte_method_audit.py`：本审计的对抗性措针脚本（本地）
- `scripts/preset_latte_semantic_boundary.py`：Latte 强编辑 + 边界状态主探针脚本（本地）

## 三个对抗性探针与客观数字

运行日志：`latte_method_audit_trace/logs/latte_audit.log`
审计数据：`latte_method_audit_trace/latte_audit/audit.json`

`edit_distance_ab = 1.49253`（Bowling[14] → YoYo[95]，CFG=7，强语义差异）。

### Probe 1 — 自洽回路甄别（loop detection）

| rank | 真边界 true | 假边界 fake（随机方向、同 rank 同量级） |
|---|---|---|
| 2   | 0.615549 | 2.02713 |
| 4   | 0.505301 | 2.05113 |
| 8   | 0.365527 | 2.08503 |
| 16  | 0.213219 | 2.10266 |
| 32  | 8.16784e-06 | 2.12655 |
| 64  | 8.16784e-06 | 2.11215 |

判定：若"误差随 rank 单调逼近 0"是投影自带的平凡性质，假边界也会跟着降。
它未降（稳定 ~2.1）。说明该下降**特定于 (zA−zB) 方向**，是真实信号，**机制不是自洽回路**。

### Probe 2 — 平凡性甄别（triviality check）

`boundary` 与"直接存低秩 (zA[:p]−zB[:p]) 缓存"逐 rank 完全一致（误差完全相同）。

判定：当前实现的边界状态在数学上**等价于压缩的前缀缓存**。这证明的是"能用低秩子空间
捕获前缀校正"——成立，但**尚未区别于"直接存前缀"**。低秩压缩本身不是 novelty；
novelty 必须来自"该子空间 = 要保护/释放的语义流形"（选择性、可解释、可跨编辑泛化），
当前探针尚未触达那一层。

### Probe 3 — 耦合逃逸确认（coupling test）

Latte 无 temporal attention（帧独立处理，仅 temp_embed 注入），因此"校准前缀不影响未来"
只是 in-latent 的便利；`full_oracle future_err == 0` 不是"编辑后重生成"的实测。

判定：当前验证只证明"能把前缀 latent 补回来"，**未回答"编辑达成 vs 前缀保持"的真实冲突**。

## 结论

- ✅ 机制是真信号（非假象 / 非自洽回路）——最关键的担心解除。
- ⚠️ 当前协议把它实现成"压缩前缀缓存"，尚未证明比"存前缀"更有内容（选择性/泛化未触达）。
- ❌ 未验证真实编辑冲突（帧独立假设绕开了前缀-未来耦合）。

## 下一步方法内检（对比与迭代之前）

加一个让校准与编辑在**同一批帧上冲突**的探针（不再前缀=确认值/未来=编辑值完全分离），
看低秩边界是否能在"保住语义 A"与"让出语义 B"之间找到子空间内的解。这一步才触及核心机制。

---

## Round-2 追加：编辑-保持冲突内检探针（2026-09-22）

脚本：`scripts/latte_conflict_probe.py`；日志：`latte_method_audit_trace/latte_conflict/run.log`
数据：`latte_method_audit_trace/latte_conflict/stats.json`
设置：同帧（prefix=6）冲突，Bowling[14]→YoYo[95]，CFG=7，seed=999。编辑范数 233.87。

### 数据（同一批前缀帧）

| method       | keep_err | aligned | residual |
|---|---|---|---|
| none         | 1.49183  | 0.0000  | 1.0000   |
| full_oracle  | 0        | 1.0000  | 0.0000   |
| boundary_rank2  | 0.5759 | 0.9225 | 0.3860 |
| boundary_rank4  | 0.4350 | 0.9565 | 0.2916 |
| boundary_rank8  | 0.2864 | 0.9814 | 0.1920 |
| boundary_rank16 | 0.1239 | 0.9965 | 0.0830 |
| boundary_rank24 | 2.86e-06 | 1.0000 | 0.0000 |

### 判定：该探针失败于设计目标 —— 未制造出真正的正交"编辑-保持"冲突

`aligned` = 边界施加的校正方向与"观测编辑方向 (zA−zB)"的对齐度，`residual` = 其正交余量。
数据里 aligned 随 rank 单调→1、residual→0、keep_err→0，这正是**构造性重合**的结果，不是选择性机制的证据：

- 本探针里"保持方向"与"编辑方向"都指向同一个向量 `zA[:p]−zB[:p]`（同一 z0、同 seed、仅换 class token 生成的两段前缀差）。二者在子空间**方向重合**，因而 rank→满秩时边界校正 = 完整前缀替换 = full_oracle，无真正冲突可言。
- 这是**负面但有价值**的方法学结论：仅在同一批帧上做前缀补齐，无法检验"选择性取舍"，因为 Latte 中保持与编辑共享同一 latent 方向。

根因仍是指向 P3 已确认的事实：Latte 帧独立（无 temporal attention），前缀保护与未来编辑本就不在同一批帧碰撞，故"编辑-保持"在 Latte 上是**构造性伪问题**。真正的冲突只会出现在**有时域耦合**的底座（如 Wan 因果注意力）上，那里前缀校准才会真正连带影响未来编辑——这才是下一步要验证的方向。

> 结论修正：当前 Latte 代理底座不足以检验"选择性释放 vs 保持"的取舍；该探针不改判 P1/P2/P3 的既有结论，但把"选择性"的验证责任彻底移交到时域耦合底座。