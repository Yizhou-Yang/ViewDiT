# 顶会评审与研究方向决策

日期：2026-09-21。

## 一、明确结论

当前成果不够作为成熟的CVPR/ICCV/ECCV/NeurIPS主会方法论文。不是只缺更多种子，而是旧方案的核心机制是标准反馈/Anderson/硬回填，评价目标有合成平凡解，真实语义收益未被证明。

本轮不再将“终端反馈的时间局部编辑”直接作为论文题目。明确选择更具体的研究问题：

**解码器感知的时间局部视频编辑：从像素帧约束到可复用latent的因果补偿。**

英文工作标题：Decoder-Aware Temporal Locality for Latent Video Editing。

这里“确定方向”指确定下一阶段值得投入且有实验证据的研究主线，不是保证顶会接收。现阶段可以自信地说机制存在、可在V100验证、已有实用补偿原型；不能说完整创新已成立。

## 二、本轮实际完成了什么

没有停留在文献审查。完成6组实验，均complete=true：

1. endpoint_stress：4个旧种子的仅拒绝/缩步长消融，以及4个新种子的三分区强基线，共32行结果。
2. wan_state_probe：官方Wan2.1 VAE、4条生成视频的解码脉冲、状态恢复与拼接等价检查，28行主要结果。
3. wan_tail_compensate：4条生成视频的因果后缀补偿。
4. wan_real_pilot：4条真实DAVIS视频×2种编辑，8个实例。
5. wan_baseline_audit：每个真实实例5项强基线或后续操作检查，40行结果。
6. wan_phase_audit：8个实例×4个终止帧位置，32项受限误差下界计算。

实际新增官方Wan2.1 VAE权重约508MB，已核验官方LFS SHA256。沿用原PyTorch环境，FP32运行，无新依赖安装、无模型训练、无完整Wan DiT部署。最后GPU进程列表为空。

真实数据为bear、blackswan、car-roundabout、camel各前17帧，来自DAVIS-Edit的原视频目录，按文件名固定选取并逐文件校验。不是只用人脸生成样例。正式实验仍需更大独立内容集合。

## 三、旧方案为何不能作为主贡献

### 3.1 收益大部分来自普通稳定化

先前有保护Anderson平均目标误差0.2216；本轮仅拒绝恶化候选并缩步长，平均误差0.2320。相对仅硬回填0.4836，简单稳定化已取得大部分收益。

这两组为相同种子和配置的跨进程对比，FP16初始误差存在约1e-4量级差异，不能把二者约4.5%的差异当作精确显著性结论。现有结果不支持把Anderson组合当独立研究亮点。

### 3.2 三分区仍被强基线挑战

4个新种子上：

|方法|核心像素MSE|编辑残差边界跳变代理|
|---|---:|---:|
|核心反馈+自由过渡|0.00013461|0.06295|
|直接编辑+自由过渡|0.00122953|0.05734|
|轨迹pinning+自由过渡|0|0.06249|
|latent淡化|约0|0.05756|
|像素淡化|约0|0.05504|

轨迹pinning用普通inpainting机制即可获得核心零误差，比额外600NFE反馈更简单。边界代理上也没有反馈独有优势。低跳变并不证明质量更高，但结果足以说明“加自由过渡区”本身不能自然补足新颖性。

## 四、更具体的机制：保护latent不等于保护像素

现代视频VAE通过因果卷积缓存编码历史。把生成状态写为：

h[t+1] = F(h[t], z[t])

v[t] = H(h[t], z[t])

其中h包括所有层的历史状态。若前面的编辑改变了h[t]，即使此后z[t]恢复原值，输出仍可能改变。

官方Wan2.1 VAE的17帧压缩为5个latent时刻。仅改变第2个latent（0-based），名义编辑输出帧5至8；帧9至16的latent没有修改，但输出发生变化。4个生成视频中后缀响应范数约为核心编辑响应范数的30.3%至33.4%。这是可测量的解码依赖，不是用相邻相关性推断的信息量。

官方整段decode与自写流式复现输出最大差为0。恢复全部边界状态使后缀误差归零，但结果与直接像素拼接逐元素相同。因此“状态恢复零泄漏”只作诊断和基线，不作新方法贡献。

## 五、已经实现的可行方法

保留已完成编辑的前缀latent，只允许修改其后的latent。在当前Wan因果解码设置中，未来latent不影响已经输出的核心帧，所以可以把后缀误差优化与核心编辑保护分开。

优化目标：

min_c ||D(z_edit_prefix, z_original_suffix+c)_P - D(z_original)_P||² / L_initial
      + 0.001 ||c||² / mean(z_original_suffix²)

- 冻结模型全部参数，仅优化每个视频的后缀latent。
- 从编辑前缀的真实解码状态开始，只反传后缀，不反传整个视频或DiT。
- Adam学习率0.03，固定24次更新，最后一次输出，不挑最优迭代。
- 标准解码器完整重放检查通过，无需修改解码器。
- 输出是latent而非只能显示的像素拼图，可保存并接收进一步latent操作。

Adam与逆问题本身并不新。值得研究的是对控制范围、时间压缩相位和像素保持之间关系的结构化建模。

## 六、真实视频结果

视频224×128、17帧，Wan VAE FP32。两类编辑分别为局部latent几何差分与矩形颜色变化再编码。它们不是文本语义编辑或精确对象编辑。保护目标是原视频的VAE重建，不是未经压缩原像素；原始重建误差另存于JSON。

|类型|案例数|无补偿后缀MSE|补偿后MSE|重编码后缀基线MSE|均值之比降幅|
|---|---:|---:|---:|---:|---:|
|几何编辑|4|0.00750981|0.00221078|0.00521700|70.56%|
|颜色编辑|4|0.00140586|0.00055914|0.00246730|60.23%|
|合计|8|0.00445783|0.00138496|0.00384215|68.93%|

8/8实例优于无补偿，也优于“像素拼接后重新编码，只替换后缀latent”的强基线。所有实例的前缀与核心编辑最大变化为0，既没有削弱编辑，也没有重算前缀。

单实例补偿阶段约11.5秒，峰值分配显存最高约6.71GB。这不是完整视频生成时延，不含加载、原始编码和所有参考缓存成本；24步均有VAE反向，不能与DiT NFE混计。

4条生成视频上的后缀误差降幅约90.6%至94.6%，显著高于真实视频。因此最终结论采用更保守的真实视频结果，不只报告好看的人脸数字。

### 强优化对照

|方法|8实例平均后缀MSE|核心保护|约耗时|
|---|---:|---|---:|
|全latent Adam，24步|0.00073519|核心/前缀平均MSE0.00239217，非零|24.45秒|
|仅后缀补偿，24步|0.00138496|最大差0|11.48秒|
|重编码热启动+后缀补偿，24步|0.00129252|最大差0|11.22秒，不含热启动编码|

全量优化后缀拟合更好，但破坏已编辑前缀，耗时更高。不能宣称我们在所有指标上胜出；这是约束与拟合精度的不同取舍。同迭代数不等于同运行时。

热启动在8例均优于零初始化，应作为后续默认候选，而不是藏起这个简单改进。热启动平均后缀MSE相比无补偿降低约71.01%，相比仅重编码后缀降低约66.36%。这些数值是本轮探索，不是独立持出结果。

### 后续操作与视觉检查

保存latent后再次对最后一个latent施加同样的小几何扰动，补偿法的既有核心保持不变。后缀重编码基线也有同样因果保持，所以“可再编辑”不是补偿独有的性质。尚未验证重新送入完整DiT语义编辑器后的行为。

检查真实熊视频关键帧可见：颜色编辑在核心区保持，结束后拖尾减弱，但未完全消失。原始编辑本身是矩形红色补丁，不能描述为自然语义修改。

所有8个案例的编辑残差边界跳变代理变大。更快恢复原视频可能造成更明显的跳变，所以降低后缀MSE不能代表过渡更自然。仍需自由过渡区、运动补偿和盲评，且不能靠模糊/消除编辑改善指标。

## 七、可证明的限制，而不是泛化过度

命题1：对确定性因果解码器，若改变的latent均在索引b以后，则b以前对应的输出不变。这来自计算图无未来依赖，可通过归纳证明。这里不是统计相关或学习出的保证。

命题2：若全部解码状态与未来latent都恢复到原序列，则未来输出等于原序列；因此整帧边界上的状态全替换可与输出拼接等价。实测4/4一致。

命题3：在“冻结latent0至2，只修改3及以后”的受限控制类中，帧0至8不可变。如果要求从帧e<9开始保护，则帧e至8已有误差的平方和，是整个保护区平方误差的下界：

L_P >= sum_{f=e}^{8} ||v_edit[f]-v_original[f]||²。

本轮8实例中，e=6/7/8时，这部分占初始保护区误差的平均比例分别为76.81%/70.61%/56.19%；e=9时为0。这解释了为什么只做后缀补偿不能直接实现任意单帧终止。

该下界只对固定前缀、未来latent补偿这一控制类成立。它不是“整个VAE无法实现帧级编辑”的证明；修改同一latent块内部通道或解码器可以改变问题。不得把优化失败当全局不可达。

## 八、与竞争工作的边界

|工作|已覆盖的内容|本方向拟争取的区别|
|---|---|---|
|Deep Equilibrium Approaches to Diffusion Models|Anderson、固定点采样与反演|不再以数值求解器本身作为贡献|
|LOCO|单步后验预测Jacobian与零空间局部编辑|研究实际时域解码与像素时间约束，需避免简单移植零空间|
|EditCtrl|局部token生成、全局上下文、高效视频编辑|不是再做一个局部编辑器，而是已有编辑的输出表示与保护约束|
|VideoCanvas|时间压缩造成条件歧义、任意帧条件、RoPE插值|其重点是条件定位；拟研究编辑之后的解码后缀补偿与硬保持限制|
|PISCO|稀疏条件、分布保持mask、实例插入|不是新的mask编码，而是原生解码输出的恢复约束|
|RefDecoder|训练参考条件解码器、细节与一致性|冻结现有解码器、优化可存储latent，控制时间影响范围|
|像素合成与轨迹回填|核心命中与范围外保持|必须承认像素输出上更简单且可精确；价值只能在需要标准latent继续处理的场景证明|

检索支持问题定位不同，不支持“无人做过”的保证。特别是“VAE有时域依赖”“优化latent以匹配像素”“因果性保证前缀”分别都是已知概念，组合后仍可能被评审认为增量。

## 九、顶会成果还差什么

不是继续堆同类MSE表。下一阶段应完成以下独立贡献链：

1. 跨至少两种视频VAE，建立时间边界相位、解码影响范围和局部可达性分析；处理归一化、双向依赖等架构差异，不滥用Wan的因果结论。
2. 将方法升级到块内控制：编辑区/保护区同属一个latent块时，采用局部响应的受约束投影或稀疏残差表示，给出有效的控制能力与失败诊断。局部线性秩不是全球不可达证书。
3. 完整语义编辑器接入，证明输出latent可被标准后续DiT流程继续使用，而不是只施加一个小几何扰动。
4. 同信息、同编辑命中、等运行时/等显存基线，以及像素合成、重新编码、全latent优化、轨迹回填。不能人为排除最强合成基线。
5. 明确评价目标是“可复用表示下的时间局部性”，同时报告过渡自然度、重建偏差、失败案例。至少30个独立内容案例，按内容聚合区间；不要把8个相关实例当大规模统计显著性。

方向选择理由：问题具体、可观测；结构限制可推导；有跨合成/真实输入的正向干预结果；单V100可完成机制与优化研究；不需要训练巨型视频主干。相比旧反馈方案，它有更明确的研究对象，但当前实现仍是原型，不应立即写成已完成方法论文。

## 十、最终决策

- 当前是否达到顶会成果标准：否。
- 旧终端反馈方案是否足够区分：否，不继续作为论文主贡献。
- 是否继续无边界地换名扫参数：否。
- 确定推进的方向：解码器感知、像素约束驱动、输出可复用latent的时间局部编辑。
- 当前首选实现：因果后缀补偿，采用重编码热启动作为强候选；它是块边界对齐时的基线能力，下一核心是块内时间控制和完整语义复用。
- 对顶会的承诺：不能保证接收；会以可证机制、强基线和真实视觉收益决定是否进入投稿，而不把数值下降包装为已证新颖性。

## 主要原始来源

- Deep Equilibrium Approaches to Diffusion Models：https://arxiv.org/abs/2210.12867
- 官方DEQ实现：https://github.com/locuslab/deq-ddim
- EditCtrl：https://arxiv.org/html/2602.15031v1
- VideoCanvas：https://arxiv.org/html/2510.08555
- PISCO：https://arxiv.org/html/2602.08277
- RefDecoder：https://arxiv.org/html/2605.15196
- LOCO：https://arxiv.org/html/2409.02374
- Wan2.1官方源码：https://github.com/Wan-Video/Wan2.1
- DAVIS-Edit数据：https://huggingface.co/datasets/AlonzoLeeeooo/DAVIS-Edit
