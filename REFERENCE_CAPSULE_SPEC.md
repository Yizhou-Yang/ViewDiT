# 紧凑参考上下文：第三轮收敛候选

## 为什么调整

双参考归一化已经改善旧版强编辑损伤，但仍难胜过最终像素拼接。继续把像素拼接当需要击败的对手会把研究推向不合理评价目标。本候选接受像素拼接作为最终输出操作，研究支撑其精确保护所需的参考信息能否被大幅缩小。

不是“可以复用latent”的新名字。复用latent的像素拼接同样可以做到，已明确作为强基线。

## 任务定义

给定参考latent z=(z_P,z_F)，参考保护像素x_P来自原标准解码D(z)。编辑后保留z_P，将z_F替换成新的z_F'。系统允许保留额外参考状态s，但不默认保存旧z_F或高分辨率x_P。

目标：从新latent(z_P,z_F')及小状态s恢复x_P，在预设误差预算下最小化附加持久存储。输出编辑区直接取原始edited decode，避免通过削弱编辑换取保护。

公平比较包括：保存旧未来latent FP32/FP16/int8，低秩压缩旧latent，保存保护像素/像素残差，压缩这些数据，以及本方法的边界统计。必须允许基线利用同样已保留的z_P、相同模型和计算预算。

如果用户系统本来已经保存完整原始视频或原latent，额外状态可能不重要，不能人为施加限制制造应用价值。更合适的应用假设是多版本分支、可替换后缀的紧凑项目状态或传输，不是一般最终视频播放。该应用需求尚未由真实工作流验证。

## 核心假设

对于某些时域压缩decoder，未来影响保护前缀仅经过有限个跨时间归一化统计。如果固定保护输入和因果上下文，原未来对保护输出的影响可由这些均值/方差构成的边界统计代表。它们是该计算图的一个充分参考状态，不声称最小充分统计或信息论最优。

储存每个受影响层/调用的mu和inverse_std。重建时从新latent执行decoder，在保护祖先节点重放这些统计。随后将恢复出的保护像素与原始edited decode拼接。

这种做法没有消除拼接，也不保证自然过渡；它减少的是为恢复旧保护像素必须保留的原未来上下文。当前实现额外执行一次受控decode，不能只报存储不报恢复成本。

## 有限条件与大小

固定CogVideoX默认两latent分批，17帧末latent编辑中受影响的是调用1的前半段，保护帧9至12；之前调用不受影响，因此不必保存它们的统计。固定37个GroupNorm调用、每组32组的均值和逆标准差，共2368个FP32标量，9472字节，即9.25KiB。

统计payload不随空间分辨率变化；层名/顺序、模型、dtype、shape、分组策略等配置仍必须一致。随着被影响边界/执行调用增多，统计状态可能增长，不能宣称任意视频长度常数内存。

对同一目标末块原latent，224×128时FP16额外存储14KiB，480×720时168.75KiB。真正有意义的是在相同恢复误差阈值下的前沿，而不是只比较元素数量。

当前FP16统计重放保护误差约1e-3，BF16可到1e-2，不满足1e-5级保护。报告FP32主结果，不能用FP16状态大小搭配FP32恢复精度。

## 可能成为贡献的部分

- 计算图层面的边界参考状态提取，识别哪些未来依赖可被统计状态概括，哪些需要高维因果缓存或根本不具此结构。
- 固定生成模型上的紧凑可替换项目表示，在明确存储/误差/恢复成本预算下对比强压缩基线。
- 双参考或在线边界归一化作为单次渲染质量/成本扩展，不能与精确拼接路线混成一个虚构指标组合。

这些只是待完整验证的论文结构。单一CogVideoX的GroupNorm状态压缩仍可能被评价为架构特定的工程技巧。

## 尚需的决定性验证

真实语义latent，而不是主要离开数据流形的几何扰动；多轮后缀替换与分支，独立内容及更多边界；第三种架构提供适用/失效对照；受控表示码率与恢复时延；与训练式压缩、常见通用压缩以及原latent量化的充分比较。

Wan严格因果模型可以作为零额外统计需求对照。不能声称对所有现代VAE都必要。第三种使用全时间注意力的模型可能不具低维统计充分性，应报告这种限制。

## 继续标准

本轮仅在已有实验支撑下将“紧凑参考状态支持可替换未来”列为更有区分度的候选主线。不宣称首次提出、普适最优、顶会完成或已获得显著语义质量收益。若跨架构与真实应用无法成立，应停止扩大为方法论文。
