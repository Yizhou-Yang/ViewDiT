# 研究决策：终端校准的时间局部视频编辑

日期：2026-09-21。

## 决策摘要

选择“已有视频的时间局部编辑：终端校准 + 上下文保护”作为下一阶段明确主线，不返回cache或固定时域滤波。V100实验证明了值得继续的控制收益，但尚未证明足够新颖的语义编辑论文贡献。

最强原型在4个新种子、2种新时间曲线上，将硬回填基线平均目标误差从0.4836降至0.2216，降幅54.18%；像素目标MSE从2.2024e-4降至7.2887e-5，降幅66.91%；目标方向增益从0.7563升至0.9762。四个种子全部改善。两个反馈方法都额外使用600NFE，约51秒。

重要限制：这是模型原生几何变化的合成目标拟合。直接输出目标合成视频即可零误差，当前结果不是超越合成、更不是语义视频质量提升54%。

## 方法介绍

输入一段生成视频、可实现的编辑和目标时间范围。先取得模型的原生编辑变化，再锁住范围外内容。控制器观察完整尾段生成的真实结果，回到检查点修正编辑扰动。

记G_s^P为带逐步原轨迹回填的75步生成尾段，xi为固定DDPM随机数，E为编辑区：

1. y_k = G_s^P(x_s + delta_k; xi)。
2. r_k = M_E(y* - y_k)。
3. 基础候选delta' = M_E(delta_k + eta r_k)。
4. 使用最多3组已接受历史的Anderson组合减少残差，岭系数0.001，组合系数L1范数不超过4。
5. 实际跑完候选尾段；终端误差下降才接受，否则减半eta、清空组合历史。
6. 固定8次尝试，失败尝试全部计费。

这不是新发明的优化器。Anderson、反馈、有限差分和回填均已有；它们在这里是工程验证手段。潜在论文贡献必须来自任务约束、跨步偏差机制和非平凡视觉收益。

## 实验链与结果

模型：Latte-XL/2，FFS，16帧256×256，DDPM250步，从第175次迭代后的检查点执行75步尾段，模型FP16，VAE逐帧解码。每条成对采样重置相同GPU随机状态；第四版全部身份复现断言通过。四组实验均complete=true。

### 第一版：任意latent目标

seed20/21，16列终端有限差分矩阵。例seed20、幅度0.2，完整响应法目标误差0.9433、方向增益0.0932。减小泄漏主要因编辑被消除，不视为成功。

### 第二版：模型原生目标

seed30/31，两种幅度。下表为4个种子×幅度条目的均值，并非4个独立种子。

|方法|目标误差|范围外相对泄漏|方向增益|
|---|---:|---:|---:|
|直接局部编辑|0.3952|0.2682|0.8048|
|完整响应矩阵+软惩罚|0.5503|0.1624|0.5440|
|硬回填|0.4181|0|0.7836|

目标可达性得到改善，但矩阵软惩罚仍削弱编辑。因此停止扩大16列矩阵方法，采用硬保留与终端反馈。

### 第三版：固定步长反馈

seed40至43，固定8次更新，无结果挑选。

|方法|目标误差|像素目标MSE|方向增益|
|---|---:|---:|---:|
|硬回填|0.4903|2.8863e-4|0.7075|
|单步pred-x0反馈|0.6534|3.6569e-4|1.1637|
|完整尾段反馈|0.4230|2.0119e-4|0.9597|

终端反馈有正向信号但不稳定。seed43从0.3143恶化到0.4760，必须报告，不能从迭代曲线事后挑最好值作为结果。单步反馈是本项目消融，不是LOCO或Frame Guidance复现。

### 第四版：有保护反馈，独立种子与曲线

seed50至53；宽渐变与双脉冲曲线。两个反馈方法从同一个hard_direct结果开始，额外尾段调用数均为8。有保护法在线拒绝恶化候选，输出当前接受状态，拒绝尝试计入预算。

|种子|曲线|硬回填误差|固定反馈误差|有保护反馈误差|相对硬回填下降|
|---|---|---:|---:|---:|---:|
|50|宽渐变|0.5543|0.5301|0.2264|59.15%|
|51|双脉冲|0.4393|0.4572|0.2346|46.60%|
|52|宽渐变|0.3947|0.5552|0.1947|50.67%|
|53|双脉冲|0.5461|0.5161|0.2305|57.79%|
|均值|两种曲线|0.4836|0.5146|0.2216|54.18%（均值之比）|

逐样本相对降幅的平均值为53.55%，与均值之比54.18%是不同统计量。像素MSE均值分别2.2024e-4、2.5278e-4、7.2887e-5。三种方法保留区像素最大误差均为0，不能将此共同机制的效果归功于反馈。

4对结果的平均绝对误差改善0.2620；探索性t区间约[0.1521,0.3720]，但仅4个种子、窄域合成任务，不据此宣称开放域统计显著。曲线类型与种子耦合，尚非每个种子覆盖两种曲线的完整析因设计。

当前消融把Anderson与接受/拒绝一起改变，不能单独声称Anderson贡献已被证明。下一步要加入“仅拒绝并缩步长”的同预算对照。

## 真实视觉与计算量

检查了seed40拼图及seed51双脉冲的第3、4、10、11帧。结果主体与背景保持，编辑主要表现为较细微的局部面部几何变化；不能将这些图描述为明显的高层语义变化或大幅质量提升。附GIF供完整动态查看。尚未完成盲评或运动/遮挡质量检验。

- 8条额外75步尾段=600NFE，有保护反馈平均51.02秒，固定反馈51.00秒。
- 完整单方法流程：base250 + native75 + initial75 + feedback600 = 1000NFE，不含身份复现检查75NFE与VAE解码。
- 若原base轨迹已有，新增750NFE；不能称实时或加速。
- 运行中一次GPU进程显存观测约5020MiB，不是正式峰值测量。
- 最后检查GPU进程列表为空，四组实验均已结束。

## 与竞品的实质区别及风险

|工作|原文机制|拟议路线的区别|不能声称的内容|
|---|---|---|---|
|TempoControl|优化cross-attention相关性、幅度与熵控制概念时间|已有视频局部编辑，终端输出校准与原内容保护|不能声称首个免训练时间控制|
|Prompt Relay|时段文本路由、边界注意力衰减|不是仅隔离文本条件，而是校验完整编辑输出|不能声称首个解决时间串扰|
|LOCO|单步后验均值Jacobian低秩语义子空间与零空间|用完整尾段响应校准，且对象是时间局部视频编辑|低秩、Jacobian、零空间并不新|
|Frame Guidance|帧条件传播与潜变量引导|拟做限制传播范围及自由过渡生成|当前single_step不是其复现|
|硬回填/合成编辑|掩码保持或输出合成|必须在未指定过渡区体现生成价值|零泄漏不是新颖性|

当前结论：任务位置可以区分，但方法新颖性尚不足以盖章。检索结果不能证明没有同类工作，尤其“终端反馈”属于广泛的优化/控制范式。

## 从原型到论文的具体路径

不继续扩大合成目标MSE表。改成三分区任务：E核心编辑区、P保护区、B自由过渡区。只规定E/P，B不提供合成标签，让生成模型处理轮廓、遮挡、非刚体形变的过渡。这使简单混合不再天然是全部目标的精确解。

首选Wan2.1/VACE-1.3B用于参考编辑与视频条件，不训练新主干。先在隔离环境完成V100 FP16、attention回退、VAE与17/33帧内存测试。现有diffusers0.31.0不是已经可用的Wan环境，尚未下载或运行Wan权重。

12例冻结试验协议见SEMANTIC_PILOT_PROTOCOL.md，含外观变化、非刚体变化、遮挡三类。必须保留硬切、像素淡化、latent混合、硬回填与同预算常规引导。只有编辑命中相当、过渡/遮挡质量优于强合成基线时，才把终端校准升格为论文方法。正式扩展按内容案例统计置信区间，不能把帧当独立样本。

建议的论文贡献目标：

1. 刻画单步代理与终端执行之间影响局部编辑的系统偏差，解释何时值得反馈。
2. 设计E/B/P约束下的终端校准，并处理时域VAE的保护边界，而不是仅组合标准求解器。
3. 建立编辑命中、范围保留、过渡质量与计算成本的完整比较，证明简单合成不能替代。

这三项是要完成的贡献，不是已经完成。当前投资决策是继续这条收窄后的路线；投稿就绪判断仍为否。

## 产物与复现

- endpoint_control.py、endpoint_native.py、endpoint_refine.py、endpoint_validate.py：四版原型。
- endpoint_*_stats.json：四组原始统计，全部完成。
- summarize_endpoint.py、endpoint_summary.json：可复算汇总。
- ENDPOINT_README.md：环境、运行方式、视频列含义、计费口径。
- ENDPOINT_PLAN.md、SEMANTIC_PILOT_PROTOCOL.md：下一阶段方法与固定案例。
- seed40至43 GIF、有标签seed51 GIF及关键帧图：真实样例。

代码已补齐到本地ViewDiT项目experiments/video，未覆盖已有同名文件；远端保留全部实验产物。打包文件不包含模型权重或大型latent张量。

## 来源

- [TempoControl](https://arxiv.org/html/2510.02226v3)
- [Prompt Relay](https://arxiv.org/html/2604.10030v1)
- [LOCO](https://arxiv.org/html/2409.02374)
- [Frame Guidance](https://arxiv.org/html/2506.07177v2)
- [Vera](https://arxiv.org/abs/2606.23610)
- [Wan2.1官方仓库](https://github.com/Wan-Video/Wan2.1)
