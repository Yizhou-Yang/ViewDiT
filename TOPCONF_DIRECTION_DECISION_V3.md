# 第三轮方向评审：从固定统计到紧凑边界参考状态

日期：2026-09-21。本文区分已经完成的证据与真实语义任务状态；任何未完成组不能作为论文结果。

## 当前判断

尚不能说已经达到成熟顶会成果标准。本轮不再只是证伪：实现了双参考边界统计传输、单次解码变体、紧凑参考状态与像素合成的组合，并完成独立新内容及480P强基线实验。

推荐集中到一个更具体的候选主线：**紧凑边界参考状态支持可替换未来的神经视频表示**。双参考归一化作为渲染精度/成本扩展，而不是单独把“固定GroupNorm”当主贡献。

明确调整：不再试图凭最终像素误差击败像素拼接。像素拼接对它自己的目标已按构造最优；本方案接受它作为输出操作，研究恢复已确认前缀所需的旧未来信息能否被小统计状态替代。

这不是换名字。新目标有独立计量：附加参考存储、恢复误差、编辑误差、恢复成本，且保留原latent量化、低秩、压缩残差等强基线。不过其真实需求及跨架构普适性仍未证实。

## 已完成实验证据

### 双参考方法与独立验证

旧方法在所有帧固定原始参考统计，大幅编辑时会改变编辑本身。本轮按保护边界分开使用参考统计与原始编辑统计。

开发：4条旧内容、17帧、224×128、0.4/1.0几何幅度、三方法24条；强幅度编辑偏差由24.14%降至3.14%。单次编辑decode版本无需edited统计预扫描，8条开发结果，强幅度偏差5.20%；它与双参考不同，不能混用精度和成本。

独立验证：预先固定8条新内容car-shadow/cows/elephant/goat/hike/horsejump-low/kite-surf/libby；33帧、224×128、两种编辑、两方法，32条配置，16个配对实例。

| 扰动类型 | 旧全局参考RMSE | 新双参考RMSE | 配对改善 | 新方法更好 |
|---|---:|---:|---:|---:|
| 强几何扰动 | 28.10% | 3.59% | 24.52个百分点 | 8/8 |
| 编码亮度编辑 | 2.10% | 0.87% | 1.23个百分点 | 8/8 |

按视频重采样10000次bootstrap，改善区间分别20.81至28.45个百分点、1.03至1.47个百分点。这仅是对旧版的诊断指标改善，不是相比竞品的感知质量显著性。

16个实例保护前缀相对受控零编辑执行均为0；相对标准decode最大差≤6.47e-6。编辑幅度比例分别99.65%至100.04%、99.88%至100.04%。几何扰动图像存在明显伪影，已人工查看，不能将保留这种变化说成语义质量提高。

### 边界状态压缩

保留仅影响跨界保护输出的归一化调用统计，而不是所有层所有时间调用。当前Cog默认执行中，一个边界需要2368个FP32标量，即9472字节，9.25KiB。

224×128下4内容×2幅度×11存储方法，共88条；480×720下2内容×2幅度×11方法，共44条。FP32统计最大前缀差在480P两内容中≤3.16e-6。FP16统计可到约1e-3，BF16更差，因此主结果使用FP32，不能混搭半精度存储大小。

### 接受像素拼接的公平参考表示比较

新方案使用统计状态恢复旧前缀，编辑区直接用标准edited decode，因此不再承受单次受控decode的编辑损伤。对照可以保存旧未来latent并标准decode恢复前缀，双方最后都像素合成。

480×720、17帧、熊与黑天鹅、9方法，共18条：

| 参考表示 | 原始payload | 两内容最坏像素误差 | 编辑区误差 | 恢复阶段平均时延 |
|---|---:|---:|---:|---:|
| FP32边界统计 | 9.25KiB | 3.16e-6 | 0 | 9.920s |
| 旧未来latent FP32 | 337.5KiB | 0 | 0 | 9.744s |
| 旧未来latent FP16 | 168.75KiB | 1.10e-5 | 0 | 9.867s |
| 旧未来latent int8 | 84.44KiB | 3.43e-4 | 0 | 9.767s |
| 旧未来latent rank4 | 8.20KiB | 0.3305 | 0 | 9.793s |
| 旧未来latent rank16 | 32.81KiB | 0.1976 | 0 | 9.768s |

FP32统计payload比FP32原latent小36.49倍，比FP16小18.24倍。zlib后的统计约8.58KiB，原latent FP32约301KiB，具体逐例数据随统计提供。

必须说明阈值敏感性：FP16原latent的最坏误差只是略高于1e-5，不能拿这个任意阈值说它失败而我们具有绝对质量优势。即使放宽阈值，它仍是有竞争力的速度/精度/存储折中，应完整呈现前沿。

这些是附加参考存储，不是总latent存储、总显存或总系统内存。恢复计时不含参考统计第一次采集、原始edited decode和模型加载。当前统计恢复与原latent恢复成本相近，没有显著加速证据。若系统已经存有原视频，额外参考压缩的价值可能很小。

简单低秩/zlib/量化并非最强学习式codec，不构成全局最小状态证明。当前仅验证一个结构家族上的充分状态。

## 理论与区分度

条件命题：当未来影响保护输出的非因果路径全部经过有限个统计节点，固定保护latent/其它祖先，并重放原统计，即可沿拓扑归纳得到保护输出不变。统计状态是一个充分上下文，而非声称最小充分统计。对未来注意力、未知条件支路、不同分块和任意块内边界不自动成立。

与近邻的关系：

| 近邻 | 已有覆盖 | 当前候选的不同目标 |
|---|---|---|
| PELC/DecFormer | 像素等价latent合成、通道混合和残差、局部分析 | 不求仅latent等价，研究紧凑附加参考状态与条件恢复 |
| LFV | 频域响应、跨VAE有效性筛选与往返漂移 | 单个时间边界的旧未来信息压缩，而非频域算子近似 |
| RefDecoder/Mirage | 解码器内参考条件与特征注入、因果结构和质量 | 冻结模型的统计级参考表示与恢复预算，不恢复未知细节 |
| LiveEdit | 因果流式语义编辑、蒸馏与mask缓存 | 不训练流式编辑器，聚焦其decoder表示的保护依赖 |
| 像素拼接＋保留latent | 精确端点、同样可再次编辑 | 本轮作为输出和强基线，比较恢复旧前缀所需参考信息 |

差异已经比“固定统计减少串扰”更具体，但尚不足以宣称完全新颖或顶会级别：参考统计、仿射归一化、图上条件非干扰均有现成基础；必须靠完整表示问题、真实任务收益与跨架构结果支撑。

## 语义链路与资源

已获取完整官方CogVideoX2b，所有LFS文件本机和远端SHA256校验。不是仅VAE实验。协议为480×720，先17帧四步资源检查，再49帧50步总时间表、strength0.6即30次有效去噪，两个连续目标“北极熊→熊猫”。共享噪声、共享DiT输出，保护帧0至12，编辑帧13以后。

两轮实验如果完成，只能称同一后缀两次改写；它不等于已经证明多块互不干扰的任意编辑组合。像素拼接保留同一latent，第二轮输入与我们相同，不能宣称再编辑是独有优势。

真实两轮任务及强度0.85补测均已完成，详见SEMANTIC_V3_STATUS.md。前两轮语义响应不完整；强度0.85补测生成可辨认白色北极熊，但姿态和背景也变化，不能算精准替换或普适语义成功。紧凑参考合成前缀最大误差2.68e-6、编辑区与原始生成结果逐元素一致。四步smoke不是语义质量证据。

本轮11组主要实验共242条配置全部完成，另有2条真实文件序列化验证。实际VCAP文件9450字节，包含元数据和布局，往返后统计逐元素一致。相同源视频两轮文件相同，不当作两个独立参考状态。

## 工程真实性

初次480P FP32 decode显存不足，先通过仅当前进程expandable_segments修复，无降分辨率/精度。再检查diffusers0.31.0发现时间核1卷积复制了永不读取的缓存；删除这些复制后，在224×128和480×720两规格编码/解码最大差均为0。480P峰值分配由32.58GB降至25.91GB。此为复现工程，不计作研究novelty。

未升级torch/diffusers。确认缺少SentencePiece后仅向研究目录安装0.2.1，由进程级PYTHONPATH加载，未改全局包。模型传输和依赖安装不算推理时延。

SMOKE新增capsule分支初版漏记额外decode时间为0，必须视为缺失而非零成本。源码已修正，运行中版本若仍为0同样按缺失处理。

其它已修复问题：数据清单先传到导致等待条件错误、macOS._文件误作JPEG、秩1矩阵字节view失败。失败日志保留，不把错误执行算进结果。

## 论文规格与下一道门

唯一建议继续的主线是紧凑边界参考状态，不再扩Anderson、弱全量回溯或原生统计优化。双参考精度与紧凑拼接是两种不同成本/误差版本，必须独立报告。

要达到可竞争顶会规格，仍需：第三种实质不同VAE、第二语义底座、至少30条独立内容与多个边界/多次替换；常见原latent压缩和表示学习基线；视频级配对统计、自然过渡/语义质量盲评；完整恢复成本和保存全部上下文的强基线。

Wan作为无需参考统计的因果对照必须保留。若其它主流模型都不需要这种状态，或实际多版本项目总存储收益很小，方向应收缩为工程/机制成果，而不是强行扩写顶会故事。

最终判断：本轮已有真实方法升级、独立验证和更可区分的研究问题；但“方向更值得做”不等于“已达到顶会规格”。不保证接收，也不把尚未完成的实验当成果。

## 原始来源

- [PELC/DecFormer](https://openaccess.thecvf.com/content/CVPR2026/html/Bradbury_Your_Latent_Mask_is_Wrong_Pixel-Equivalent_Latent_Compositing_for_Diffusion_CVPR_2026_paper.html)
- [LFV](https://arxiv.org/html/2608.07569)
- [LiveEdit](https://arxiv.org/html/2606.26740)
- [Mirage](https://arxiv.org/html/2512.24227v1)
- [RefDecoder](https://arxiv.org/abs/2605.15196)
- [CogVideoX2b官方模型](https://huggingface.co/THUDM/CogVideoX-2b)
- [DAVIS-Edit](https://huggingface.co/datasets/AlonzoLeeeooo/DAVIS-Edit)
