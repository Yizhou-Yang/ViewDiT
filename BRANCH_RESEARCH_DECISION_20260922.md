# 分支耦合研究决策

日期：2026-09-22。状态：未确认任何新方法达到顶会贡献标准。

## 结论

停止把“共享随机性／共享隐变量使多个编辑分支更一致”作为当前主贡献继续扩大训练。理由是基础构造已有近邻、当前标准匹配没有超过同种子、且所用无训练CogVideoX V2V尚未满足源保持与编辑成功的联合门槛。这个资源决策不等于对所有可学习耦合的普遍证伪，也不等于证明视频编辑方向不可行。

不能从这轮推出“训练无用”。本轮没有训练候选。此前LoRA资源探针表明训练可行，但训练资源可行与方法新颖有效是两件事。

## 完成的真实V100实验

### 原生空间分辨率复核

远端结果：`/root/viewdit/results/video/branch_coupling_native_gate_20260922_run2`。

CogVideoX-2b，FP16 DiT、FP32 T5/VAE；17帧480x720，50配置DDIM步、strength0.75对应37实际步、CFG6、eta0。bear/bus两开发场景，每场景两个目标caption、两个固定seed，共8个输出，全部完成并解码有限。每段去噪约53.95至54.55秒，不含模型加载、编码与解码。

| 场景 | 同种子奇数帧边框MSE | 随机双射期望MSE | Hungarian配对MSE | 超过同种子的收益 |
|---|---:|---:|---:|---:|
| bear | 0.03492288 | 0.10614175 | 0.03492288 | 0% |
| bus | 0.00916878 | 0.05030109 | 0.00916878 | 0% |

两场景assignment均为[0,1]。相对随机双射分别降低67.10%、81.77%，但这是标准同种子基线已达到的结果，不能归因于新方法。

视觉检查固定seed0的0/8/16帧，以及全部8个输出的frame8，无挑选最佳种子：

- bear：恢复可辨识的动物和岩石场景；白熊和熊猫目标有不同程度响应，但源背景被改写，姿态/构图有变化，seed0熊猫仍有大面积棕色皮毛。
- bus：seed0有局部红/黄边饰差异，但没有变成完整目标颜色，车辆和街景结构有改写；seed1红/黄两分支近似，基本没有完成目标换色。
- 480P避免了之前低分辨率动物样例中的严重条纹崩坏，但没有单因素证明低分辨率是唯一根因，也没有证明全视频质量全面恢复。

限制：只有2个开发场景、每分支2种子；外圈20%是背景代理而非真实mask；相邻奇偶帧强相关，不是独立场景测试；没有盲评或语义成功率统计。

### 编辑强度诊断

远端结果：`/root/viewdit/results/video/branch_strength_diagnostic_20260922`。

固定bus、seed20261001、原生空间尺寸和同一源latent/文本，新增strength0.35和0.50各红/黄两个输出；复用0.75对照。4个新输出全部完成，未重采样。

| 强度 | 实际步数 | 红分支源MSE | 黄分支源MSE | 固定帧视觉判断 |
|---|---:|---:|---:|---|
| 0.35 | 17 | 0.01369145 | 0.01353502 | 更接近源视频，但基本未换成红色/黄色 |
| 0.50 | 25 | 0.02683802 | 0.02645938 | 源结构相对保留，仍基本未完成换色 |
| 0.75 | 37 | 未在本表混入跨seed均值 | 未在本表混入跨seed均值 | 局部换色但结构改写明显 |

源MSE不是编辑成功度。降低强度不是本例中兼得编辑成功与保真的解法。一次开发样例诊断不证明所有采样配置都无效，不构成SOTA对比。

## 工程问题及恢复

原生首次运行在采样前因CPU VAE/CUDA DiT混用时自动设备推断错误失败。原生脚本以`_execution_device = transformer.device`修复后完成；低分辨率本地脚本也已同步修复，但旧结果对应旧hash，远端旧脚本未覆盖。

强度诊断完成全部采样后在FP32 VAE解码时OOM。已从保存的4个latent在新进程中恢复解码，并设置进程级`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`。未改变采样轨迹，未使用空间tiling，未修改VAE算术。恢复脚本`branch_strength_decode.py`；最终stats complete=true。恢复成功不能单独归因于分配器，因为同时改变了进程生命周期。

最后查询GPU为0MiB、0%利用率，无本轮残留训练或推理任务。没有下载Lucy权重、安装新环境、commit或push。

## 新颖性审查补充

核心既有近邻详见BRANCH_NOVELTY_AUDIT_20260922.md。

本轮进一步下载并核对CNA原文：https://arxiv.org/html/2609.18488v1 。它优化训练时数据-噪声配对，使用对比目标、角熵和径向约束，正文明确为approximately Gaussian/relaxed prior；有限维、有限batch下不是逐条件精确边际保持。结论部分仍将conditional/latent generation列为后续方向。这与固定编辑器的跨条件精确耦合不同，但不能宣称“学习Gaussian噪声语义对齐”本身首次。

进一步核对Memory-V2V原文：https://arxiv.org/html/2601.16296 。其文本编辑检索依据对应源片段的DINO视觉相似度，并训练LucyEdit接收源视频与历史视频；附录已包含memory-token Gaussian扰动、插入moon后跨视角重访、对象消失再出现。因此“记忆当约束”“记忆抗噪”“遮挡后恢复”不能作为未被覆盖的主张。没有复现官方模型，不能宣称其在我们的任务失败。

新增相邻线索：InfinityEdit研究不断到来的指令与未来片段延续（https://arxiv.org/abs/2608.20910）；MMLVE官方页面已有跨镜头一致性、多指令解耦和全局视觉anchor（https://wucy0519.github.io/MMLVE/）。本轮只核对摘要/官方页面，不声称完整审查所有实验。

选择性撤销在协同编辑系统已有长期文献。针对生成视频的检索未找到直接充分证据，不代表学术空白；不能以换一个领域名建立novelty。

## 正面下一路线：可修订视觉约束，仅列为待验证假设

优先探索的问题不是“让所有历史更一致”，而是“新编辑使旧视觉证据部分失效时，识别哪些约束仍应保持，哪些必须重建”。例如在重定位物体后，保留材质与身份，但不能保持原位置的阴影；撤销物体插入时，应清除其投影和反射，又不能回滚之后独立添加的内容。

可实现假设：输入源视频、当前视频、结构化编辑记录和历史视觉tokens，训练一个操作条件的token有效性路由器，输出retain/recompute/irrelevant门控，与小规模LoRA联合学习。源事实与生成历史分开编码；门控同时受实体、时空对应和操作依赖监督。不是线性残差相加，也不强制所有旧帧像素保持。

这仅是候选机制。非线性门控、图依赖、约束失效都是已有思想；只有在相同输入信息与训练数据下超过简单规则/历史筛选/普通联合网络，才可能形成新的视觉学习贡献。原来的四世界差分只作为诊断，不宣称提供额外监督信息或因果可识别性。

### 执行顺序和停止条件

1. 先部署一个有效指令编辑器基线。Lucy-Edit-Dev为优先待验证选择，需隔离依赖并实测V100 FP16/offload；官方BF16示例不能直接当可行性证明。若资源失败，保留日志，改用已公开可运行的小型编辑基线，而非在坏输出上继续研究。
2. 先固定少量开发任务：无冲突保留、同实体属性覆盖、位置改变引起派生效果失效、选择性撤销。检查单轮成功，不能把单轮失败归因于历史记忆。
3. 必须比较：仅当前视频、全部历史、最新相关历史、按实体/属性简单规则筛选、从源视频重放剩余有效指令、同信息同数据的普通可训练注意力模型。重放成本计入，不能给候选独占结构化日志或额外监督。
4. 使用可控配对视频提供派生效果标签，但训练/验证/测试按场景及操作组合划分；真实视频再做盲评。不能只用两个球的解析渲染宣称现实视频效果。
5. 先证明简单规则和重放仍有剩余缺口，再训练路由器/LoRA。主评价同时报告编辑成功、独立已接受编辑保留、应失效效果清除、非目标区域及时间质量、总成本；不得压成单一有利相似度。
6. 若优势仅来自给候选更多历史信息，或只是规则筛选能解释，停止该方法；若只有玩具收益或真实编辑不成功，不扩大训练。确认性效果门槛和样本量应在开发方差估计后预注册，不根据测试结果反向选择。

当前没有该新候选的训练结果，没有证据确认其novel/significant，也不承诺任何录用结果。
