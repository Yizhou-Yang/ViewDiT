# 冻结视频模型的边界参考状态：竞品审计、数学与方法调整

日期：2026-09-22。研究版本：V4。本文是研究设计与证据审计，不是宣称已经完成的顶会论文。

## 0. 结论先行

当前方法不能以“参考归一化统计重放”“不训练的局部编辑”或“9 KB恢复视频”作为独立顶会创新点。新检索确认两项此前遗漏的直接先例：SIGE已在编辑推理中重用原始归一化统计；Tiled VAE已收集并复用GroupNorm统计。这比仅讨论AdaIN更关键。

建议保留研究对象，但改变主贡献目标：从单个GroupNorm技巧，推进为“面向已提交视频前缀的、依赖感知的参考状态表示与恢复”。核心问题是：在保留前缀latent与冻结模型的条件下，哪些旧未来信息可以丢弃，仍然恢复已经确认的前缀像素？如何检测状态失效？如何在码率、失真与恢复计算之间做公平选择？

本轮已新增可运行的边界契约原型，支持审查过的Cog执行配置、多切点、零状态分支、前缀与配置失效检查。自动计算图编译、混合状态优化、多提交组合及第二个有正收益的架构尚未实现。不能把这些计划写成已经完成的贡献。

“保证不和任何现有工作撞车”无法通过有限检索证明。能做到的是：对已经找到的重合主动承认，逐条收窄主张，并设置明确的继续与停止条件。

## 1. 首先纠正之前的说法

### 1.1 正确的时间方向

实际任务是修改后面的未来内容，保护前面已经确认的过去前缀。此前通俗解释把前后方向讲反了，现撤回。不是改前半段后，仅靠9 KB重建丢失的整个后半段。

正确例子：49帧视频的0至12帧已经确认；我们改写13至48帧。保护前缀对应的latent仍保留，但它解码出来的像素可能被未来块的归一化统计影响。我们保存的不是画面全部信息，而是这种额外影响。

### 1.2 权重冻结不等于解码不变

我们没有训练新VAE，也没有修改其权重；但运行中替换了部分归一化的统计来源，因此解码函数的执行已经改变。准确说法是“frozen weights with controlled decoder execution”。之前Abstract中的“never alters model weights or decoding”不成立，应删除后半句。

### 1.3 双参考与紧凑合成不是同一个版本

双参考方法在保护区用旧统计、编辑区用新统计，仍可能改变编辑区的输出。紧凑参考合成则用受控解码恢复前缀，再把原始edited decode的后缀直接拼回，因此编辑区误差按构造为0。不能把双参考3.59%的指标、合成版的零编辑误差和单次解码成本合并成同一个方法。

### 1.4 9 KB不是完整视频码率

9.25 KiB是2368个FP32统计标量的payload。恢复还需要保留的前缀latent、共享VAE权重、执行配置，以及相关计算。旧337.5 KiB基线是480×720配置下一个必要旧latent块的FP32大小，不是整个视频、也不是全部未来的latent栈。

先前28.10%到3.59%的“编辑偏差”是相对标准edited decode的归一化RMSE，来自强几何扰动。它不是语义错误率，不是用户编辑成功率，更不是视频质量提升87%。

## 2. 竞品审计方法

检索覆盖：参考统计、归一化重放、tiled VAE、增量生成、像素等价latent合成、条件视频解码、流式编辑、带侧信息的函数编码及检查点重计算。既查论文，也查公开工程源码；工程先例同样可能使主张失去新颖性。

证据层级：A表示本轮读取官方论文正文或官方源码；B表示核查官方摘要、作者主页或权威出版元数据；C表示仅作为检索线索，不用于实测优劣判断。本文没有运行竞争方法的官方基准，因此所有竞品性能数字若提及，只能归属于其论文，不能混作同硬件实验。

## 3. 最重要的直接近邻

### 3.1 SIGE：最危险的编辑场景先例

论文：Efficient Spatially Sparse Inference for Conditional GANs and Diffusion Models；NeurIPS 2022，后有T-PAMI扩展；arXiv:2211.02048。[S1，A]

原文III-B的“Pre-computing normalization parameters”明确指出：在小幅编辑下，原始和编辑激活近似相同，因此重用原始激活的均值和方差，将归一化替换为预计算的Scale+Shift。实现说明还明确在扩散模型中对所有GroupNorm复用原图统计。

简化机制：对线性卷积F，有F(h+Δh)=F(h)+W*Δh；缓存F(h)，只算稀疏变化。归一化统计则从原执行复用，以避免全图重新统计。注意：线性卷积恒等式是精确的，整个有归一化与非线性的编辑管线并不因此精确等于原始完整推理。

与我们的重合：冻结权重、编辑上下文、原始统计重用、仿射替代、旧结果复用。这些不能再申报首创。

尚可区分的问题：SIGE的主要目标是小编辑下的计算加速，依赖缓存激活；我们的候选是已知结构条件下，即使允许的未来变化很大，仍恢复历史前缀，并减少持久参考信息。不保存高维激活并不是天然优势，它通常意味着多做恢复计算，必须公平展示存储与时延。

必须做的对照：同一Cog输入上的“全参考统计重放”、只保留必要参考latent、缓存必要边界激活、参考像素拼接。对SIGE思想适配的实现应标为“SIGE-inspired reference-stat replay”，不能冒称官方SIGE视频复现。

### 3.2 Tiled VAE：统计状态机制的直接先例

来源：pkuliyi2015的Tiled VAE及ComfyUI-TiledDiffusion实现。[S2、S3，A/B]

其快速路径先对缩小的输入估计各层GroupNorm统计，后续图块重用这些统计；普通路径在归一化处暂停、汇总图块统计再继续。官方源码GroupNormParam.from_tile保存mean/variance，并返回固定统计的归一化函数。机制与我们的均值/逆标准差重放高度重合。

区别不是“我们用视频，它用图像”这么简单。可研究的差异是：Tiled VAE希望重建同一输入的分块推理结果；我们希望在旧未来内容被替换后，恢复特定旧输出，状态还必须绑定提交对象和允许编辑集合。前者减少一次推理的工作内存，后者减少跨编辑持久存储。

如果最终只做“把Tiled VAE统计保存到文件并用于视频”，创新强度仍不足。需要至少完成结构化依赖提取、非干扰适用性判断及完整项目收益中的实质贡献。

源码许可也应注意：第三方Tiled VAE实现有CC BY-NC-SA等许可说明。本轮代码按既有数学公式独立编写，没有复制其实现；正式复用代码时必须重新核查许可。

### 3.3 AdaIN与StyleAligned：操作不相同，但创意空间已被覆盖

AdaIN（ICCV 2017）定义：[S4，B]

$$\operatorname{AdaIN}(x,r)=\sigma(r)\frac{x-\mu(x)}{\sigma(x)}+\mu(r).$$

它先按当前输入标准化，再对齐到参考风格的均值和尺度。我们的固定统计重放是：

$$N_{s_r}(x)=\gamma\frac{x-\mu_r}{\sqrt{v_r+\varepsilon}}+\beta.$$

二者不应简单说成完全一样；但“以参考统计控制特征”“不同区域用不同参考”“参考混合与插值”等都已有先例，不能仅靠改变公式中的统计来源主张强创新。

StyleAligned（CVPR 2024）对目标与参考的queries/keys进行AdaIN，并共享参考attention，实现无需微调的风格一致性。[S5，B] 因此“冻结扩散模型、运行时参考注入、无训练”也不是独有卖点。

### 3.4 PELC／DecFormer：像素等价目标已有人系统提出

论文：Your Latent Mask is Wrong: Pixel-Equivalent Latent Compositing for Diffusion Models，CVPR 2026。[S6，A/B]

设像素合成为C_M(x_a,x_b)=M⊙x_a+(1-M)⊙x_b。PELC关注学习latent算子F，使：

$$D(F(z_a,z_b,M))\approx C_M(D(z_a),D(z_b)).$$

DecFormer预测逐通道融合系数及残差，在冻结VAE监督下训练轻量合成器。无需微调主扩散模型并不等于整个方法无训练。

我们的紧凑合成并不产生一个能由原生D独立解码的像素等价latent；输出依赖侧状态和受控解码，最后还可能像素合成。因此它不是PELC的同接口替代品，也不能声称解决了其native latent equivalence问题。

如果未来改回训练latent校正器去逼近像素合成，便重新进入PELC核心范围，必须进行更直接的算法比较。

### 3.5 LFV：筛选与fallback不能重新包装成创新

论文：Latent-Frequency Validity: Fast Spectral Editing with Screened Video-VAE Transfer Operators，arXiv:2608.07569。[S7，A]

LFV在视频VAE的频域编辑中评估解码目标拟合和往返漂移，选择逐频逐通道校准C1、完整通道混合CM，或回退reference branch。其“可行性筛选、能力阶梯、跨VAE有效区域、失败回退”已形成完整框架。

我们不是频谱算子校准，而是已提交输出的参考信息与依赖切断问题。若只把“先测试，成功就用，不成功就回退”换成boundary validity，仍然缺乏方法创新。候选差异必须是结构性路径覆盖与状态条件，而不是仅用少量测试样本做经验筛选。

## 4. 其它重要近邻与非直接基线

### 4.1 RefDecoder

RefDecoder: Enhancing Visual Generation with Conditional Video Decoding，arXiv:2605.15196。[S8，A] 使用轻量参考图像编码器和reference attention，在各级上采样中注入高维参考token；它训练条件解码机制，可换入已有生成系统。其目标是细节与一致性，而不是精确恢复一个已确认前缀的低码率状态。

禁止说“首次在视频解码器内加入参考信息”。应比较参考输入大小、是否训练、恢复目标、允许编辑集合与完整成本，而非只说“我们的参数少”。

### 4.2 Mirage资产编辑

这里特指Mirage: One-Step Video Diffusion for Photorealistic and Coherent Asset Editing in Driving Scenes，arXiv:2512.24227。[S9，A] 不是Decart的同名产品、不是同名语义视频传输方法，也不是另一篇latent spatial memory工作。

它将2D编码器的时间无关特征注入3D VAE后期层，配合Reconstruction-LoRA与Harmonization-LoRA，保持因果结构并改善资产细节。故“因果解码器的参考注入”已被覆盖。我们的目标是保持旧输出不被未来改写影响，不是资产真实性提升。

### 4.3 LiveEdit

LiveEdit: Towards Real-Time Diffusion-Based Streaming Video Editing，arXiv:2606.26740。[S10，A] 三阶段蒸馏把双向编辑能力转到因果流式编辑器，并有AR-oriented mask cache。它主要控制在线语义编辑、时延和内容保持。

我们当前约一次额外VAE恢复，不应称实时编辑器，更不应把保护性与流式能力都当首创。二者可组合，但组合可能性不证明新增贡献。

### 4.4 Checkpointing与函数编码

检查点与重计算早已研究“存哪些中间量，花多少重算代价恢复”。[S11，B] 我们不能把存储—计算取舍或graph cut本身当新算法。

Coding for Computing研究接收端有侧信息时，发送多少数据即可计算目标函数。[S12，B] “不用重建全部旧latent，只保留完成某个函数所需的信息”是已有理论思想。我们的增量若成立，应是针对实际神经视频执行图的可构造状态、适用边界及实证，而不是重新命名函数压缩。

### 4.5 真正需要打败的朴素基线

最强对手不一定是最新论文：只保留必要旧latent块并压缩；只保留受影响的少量旧像素帧；保存共享参考一次并让所有分支指向它；移动切点到天然无泄漏的块边界；保存源输入和生成配方后重算。

前缀本来严格因果的模型或切点应使用零额外恢复状态。为了让方法显得有用而强制给这类情况加状态，是不公平评价。

## 5. 统一数学定义

令D_{θ,q}为权重θ、执行配置q下的确定性解码器。q包括分块、时域填充、dtype、tiling、模块顺序和必要算子实现。将原latent分为保留部分u与可替换部分v：z=(u,v)。令P抽取要保护的像素，Q抽取编辑区，P与Q不重叠且覆盖目标视频。

参考像素：y_r=P D_{θ,q}(u,v_r)。新的未来为v_e，原始edited输出为x_e=D_{θ,q}(u,v_e)。我们允许保存侧状态s=E_s(u,v_r,θ,q,P)。恢复器输出R(u,v_e,s)。

目标：

$$\|P R(u,v_e,s)-y_r\|_\infty\le\epsilon_P,\quad Q R(u,v_e,s)=Qx_e.$$

要优化的是：

$$\min_s\; B_{\rm actual}(s)+\lambda T_{\rm restore}(s),$$

约束应明确是“所有允许编辑v_e”还是“指定测试分布中的编辑”。前者需要结构性证明和数值界，后者只是经验有效。两者不可互换。

通俗理解：u是仍保存的画面底稿；s不是另一份视频，而是让这份底稿恢复原来解码解释所需的补充说明。

## 6. 为什么改未来会影响过去

考虑某一GroupNorm组，摊平后h∈R^n，包含通道、空间和该次调用的时间位置：

$$\mu={1\over n}\sum_jh_j,\quad v={1\over n}\sum_j(h_j-\mu)^2,\quad r=(v+\varepsilon)^{-1/2}.$$

$$y_i=\gamma_i(h_i-\mu)r+\beta_i.$$

即使卷积只看过去，若均值与方差包括未来位置，未来h_j改变后，过去y_i的缩放与偏置也会改变。

对i≠j，求导得到：

$$\frac{\partial y_i}{\partial h_j}=-{\gamma_i r\over n}-{\gamma_i(h_i-\mu)(h_j-\mu)r^3\over n}.$$

推导：∂μ/∂h_j=1/n；∂v/∂h_j=2(h_j-μ)/n；∂r/∂h_j=−r³(h_j-μ)/n；代入乘积求导即可。

把保护位置I与未来位置J分开，这个跨区Jacobian是两个外积之和，所以单组单层的跨区秩至多2。直观上：未来通过“中心”和“尺度”两个数影响过去。

重要边界：这是GN已知公式的推论，不是全新数学；也不证明整个深网只需要两个数。不同组、不同层和其它跨区路径都会增加信息量。局部Jacobian低秩也不自动推出任意大变化的全局低维精确编码。

## 7. 参考状态为什么在条件下足够

### 命题一：路径覆盖下的充分性

将实际前向执行展开为有向无环图。假设：保护latent祖先u固定；每条从可变未来v_e到保护输出的路径，要么不存在，要么经过某个可替换统计输入；这些统计输入全部固定为参考执行记录s_r；其它条件与运行配置固定。

则在实数算术下，受控执行的保护输出与参考执行相同，对所有允许的v_e不变。

证明：把选定统计节点视为常量输入。从输入开始按拓扑顺序归纳，保护输出祖先中的每个节点只依赖已固定的祖先和常量统计。于是它们逐个与参考执行相同，最终保护输出相同。若还有未覆盖的未来路径，这个证明立即失效。

通俗解释：未来到过去的每条传话通道都必须被切断；只堵住其中一条不够。

这是一项条件保证，不是声称当前程序已经能自动证明任意VAE。单凭“查找所有GroupNorm”既看不到functional normalization，也不能排除attention或其它未来条件路径。

### 命题二：零状态的必要基线

若P D(u,v)本来就与v无关，则s为空即可，附加恢复payload为0。严格因果且边界正确映射的解码器属于这一类。同一模型不同切点也可能有不同结论。

本轮针对diffusers 0.31.0的Cog默认解码，奇数latent长度与batch size=2时调用分块为[0,1,2]、[3,4]、[5,6]、[7,8]。以latent索引b为首个编辑位置，b=3、5、7是分块起点；b=4、6、8穿过一个分块内部。预测前者无需统计状态，后者需保留该次跨界调用统计。这是审查过的配置规则，不是任意视频的奇偶定律。

### 命题三：最小状态不能只靠数维度声称

固定u，定义两个旧未来v_a与v_b等价，当且仅当P D(u,v_a)=P D(u,v_b)。任何精确恢复编码都必须区分产生不同目标输出的等价类。若只考虑有限个可区分目标，数量为K，则固定长度无损状态至少需要⌈log₂K⌉比特。

证明：少于K个码字时，至少两个不同目标共享码字；相同u和码字会使恢复器给出同一个结果，无法同时正确。这个鸽巢论证不能推出我们的9472字节最优，也不能用连续实数维度代替真实比特数。

更一般的可微因子分解F(v)=g(s(v))满足rank(J_F)≤dim(s)。该界只适用于所述可微连续编码，不排除非连续或有限精度编码技巧，也不是完整的码率—失真定理。

## 8. 为什么不能直接进行统计加减更新

一种诱人的扩展是：未来变了，只更新它的均值方差，不重新执行网络。对同一层、互不重叠的集合，原始矩可以精确合并：

$$N=N_A+N_B,\quad \mu={N_A\mu_A+N_B\mu_B\over N},$$

$$v={N_A(v_A+\mu_A^2)+N_B(v_B+\mu_B^2)\over N}-\mu^2.$$

但跨过非线性之后，这些矩通常不够。反例：a=(−1,−1,1,1)，b=(−√2,0,0,√2)，两者均值0、方差1。经过ReLU后均值分别为0.5与√2/4，已经不同。

因此“旧均值方差＋新均值方差即可精确更新所有层”一般错误。未来若要加入增量矩更新，应逐层重算必要激活，或声明并检验明确近似，不能把输入层矩的可加性推广到整个VAE。

## 9. 双参考方法的准确定位

设r_r=(v_r+ε)^(-1/2)，a_r=γr_r，b_r=β−γμ_r r_r；编辑参考同理得到a_e,b_e。令m_t为编辑权重，则：

$$y_t=[(1-m_t)a_r+m_ta_e]h_t+(1-m_t)b_r+m_tb_e.$$

在保护祖先上m_t必须为0。若软过渡把m_t>0扩到保护区域，则原非干扰条件不再成立。编辑区用原始edited统计也不能保证最终edited像素完全一致，因为更早的受控层可能已经改变卷积上下文。

因此本文把双参考列为渲染误差与成本的扩展项，而不作为主novelty。主版本使用：

$$x_{out}=M\odot\widetilde D(u,v_e;s_r)+(1-M)\odot D(u,v_e),$$

其中M是保护区二值掩码，受控解码只负责恢复参考区。编辑区等于标准edited输出是公式构造的结果；它不证明边界自然、语义成功或整体画质优于像素合成。

## 10. 方法调整：从固定胶囊到边界契约

“契约”仅指可检查的复用条件，不代表已获形式化认证，也不声称该名称本身新颖。

### 10.1 当前新增原型的行为

提交阶段：检查审查过的配置；计算权重标识；绑定保留latent前缀摘要、时间切点和shape；根据分块规则选零状态或必要的一次归一化调用；捕获参考输出与统计。

编辑阶段：允许任意替换定义内的未来latent，但前缀必须原样保留。

恢复阶段：先校验权重标识、执行配置和保留前缀；不匹配就报错要求重新提交，而不是静默使用旧统计。零状态边界直接返回raw；跨统计边界执行参考恢复，再与raw编辑区拼接。

本轮恢复支路对选定跨界调用的所有时间位置使用参考统计，最后只取其保护前缀；不追求该恢复支路编辑区的质量，因为最终编辑区来自独立raw。这个简化不能被误叫成双参考模型。

实现仍使用手工审查的Cog规则；权重摘要由调用者在不可变模型会话开始时计算，模型发生改变后必须重新计算。摘要不是抵抗恶意修改的完整安全认证。没有自动检查隐藏attention或任意自定义hook。

### 10.2 下一步真正有研究价值的扩展

候选扩展A：从实际执行图中提取未来到保护输出的跨区路径，区分可由低维统计表示的路径与必须保留高维状态的路径。未被支持的算子明确返回unknown，而不是默认安全。

候选扩展B：在统计、必要latent、边界激活、像素残差之间选择参考表示。目标是在固定误差预算下，降低实际存储并约束恢复时延，不是让所有模型都输出一个9 KB包。

候选扩展C：处理多次提交与分支的状态依赖。一个版本的已显示前缀可能由多个旧参考片段组合而成，并不等于D(z_current)的前缀。直接对当前latent重新捕获统计，可能把用户确认过的显示结果悄悄改变。必须保留区间与提交依赖，并让commit依据已确认渲染，而非假定latent天然代表它。

以上三项是候选研究增量。若只有配置校验和文件格式，则仍是良好工程，不足以支撑顶会方法贡献。

## 11. 混合状态优化的数学规格

把未来到保护区路径记为集合Π。每个候选切断位置j可存统计s_j、完整必要激活h_j，或通过保留latent进行重算。为简化问题，先固定每种选项的独立比特成本b_j与保守误差上界e_j。

定义选择变量x_j∈{0,1}，基本路径覆盖条件：

$$\forall\pi\in\Pi,\quad \sum_{j\in\pi}x_j\ge1.$$

在只有独立节点成本、固定图和精确替代的理想模型中，可归结为带成本的节点切断问题；不要把现成min-cut算法叫成新算法。真实问题还包含共享重算、非加性压缩、多个状态粒度及图外执行条件，不能未经证明称一个普通min-cut就全局最优。

对近似状态，局部线性化为：

$$\Delta y_P\approx\sum_j J_j\Delta s_j.$$

在受约束域内若有可用的Lipschitz界L_j，则可用Σ_j L_j||Δs_j||作为保守误差预算。只通过Jacobian抽样估计的L_j不是全局证书。

以高码率量化近似e_j≈κ_j2^{-b_j/d_j}，可以设计分配比特的启发式并随后验证，但必须标为近似优化。本轮没有实现或验证自适应量化，不能提前报告存储提升。

正向落地顺序：先实现精确的统计/完整状态两档，再加入有限精度压缩；先让路径覆盖与复用条件可审计，再研究更激进的小状态。

## 12. 多次编辑为什么不能只说“可组合”

若所有轮次都固定同一个u和同一个参考目标y_r，则同一个s_r可以服务多种未来v_e。这只是“同一前缀下重复换后缀”。

若第k轮又确认了新的区间，新的目标会包含第k轮呈现的画面，而不是最初参考画面。需要状态对象包含：区间、提交父节点、latent引用、执行配置、参考状态以及渲染版本。恢复必须针对各区间各自的目标，不能简单合并两个均值方差包。

建议先做单链提交：protect(0:13)→edit(13:)→commit(0:21)→edit(21:)。逐轮比较此前所有已提交像素；加入回退再分支。之后再做非相邻空间mask，不要从前缀性质直接外推任意掩码。

候选组合定理也需要所有旧保护祖先仍固定且新干预不穿越旧保护依赖闭包。如果不能满足，必须重新生成合法状态或保存残差。不存在仅靠“都是局部编辑”就保证交换律的普适结论。

## 13. 数据与证据边界

### 13.1 既有V3证据，不冒充本轮新增

8条V3独立内容、33帧上的强几何扰动：全参考重放编辑相对RMSE约28.10%，双参考约3.59%。这些内容本轮再用时已经属于开发集。

480×720、17帧两内容参考表示比较：统计FP32 payload9472字节；必要旧单块latent FP32为345600字节，FP16为172800字节；统计恢复最大误差约3.16×10^-6，编辑区经合成误差0。这里只测试一个VAE家族。

真实CogVideoX 49帧的低强度两轮生成，目标语义不完整；高强度单开发样例得到可辨认白色北极熊，但背景姿态也变化。前缀恢复误差约2.68×10^-6。原生分辨率前缀恢复阶段约9.9秒，不包括raw、初次参考捕获或模型加载。

实际9450字节VCAP文件经过精确统计往返验证，但其旧schema不包含本轮完整复用契约，不能把旧9450字节当成本轮含所有校验信息的新文件大小。本轮新契约暂驻内存，仅报告统计payload，不虚报序列化码率。

### 13.2 本轮V4验证定义

设备为V100，FP32 VAE，不变权重。复用car-shadow、cows、elephant、goat四内容，33帧，128×224。切点b∈{3,4,5,6,7,8}，每个切点两次固定种子的高斯后缀扰动，共48个编辑实例。

这不是语义编辑基准，不是新独立泛化集，也不是多轮提交。检验对象是：跨界切点选取、同状态重复使用、零状态基线、前缀/模型/分批失效检查及GN导数公式。

最终具体结果由附录“V4实测结果”从complete=true原始JSON写入。未完成时不填预期数字。所有失败应保留，不通过筛选切点提高成功率。

## 14. 如何证明significance而不是只报36倍

实际项目包含共享存储B_shared、当前latent B_current、参考状态B_ref及元数据B_meta。比较应为：

$$B_{project}=B_{shared}+B_{current}+B_{ref}+B_{meta}.$$

以49帧、13个latent时间步、每块FP32 337.5 KiB为纯张量例子：当前latent为4387.5 KiB。再保存一个旧块，总4725 KiB；改存9.25 KiB统计，总4396.75 KiB，项目张量总量只下降约6.95%，不是36倍。这还是未计模型、元数据等的示意计算。

因此36倍仅是被替代的附加参考项之比。如果用FP16当前latent或有压缩视频，比例还会改变。若模型权重已共享且项目很大，不能强行把共享权重计给某个基线；应分别报含共享与不含共享口径。

N个后缀分支若共享同一保护前缀，则只需保存一次参考旧块或一次状态，不应让基线保存N份旧块来人为制造线性收益。需要许多不同已提交上下文，才可能真正累积参考存储成本。

若用户接受切点移动到天然块边界，零状态方案可能支配我们。必须报告“时间控制精度—额外状态—恢复时延”的前沿，说明精确帧边界为何重要，而不是把所有切点都视为同一任务。

## 15. 顶会规格的实验计划与停止条件

第一关，直接先例差异：把SIGE与Tiled VAE的操作重合写进Related Work；证明新增的路径分析/状态选择/提交语义不只是统计文件化。失败则不按独立方法论文继续包装。

第二关，结构泛化：至少两个真实有状态收益且结构有区别的解码器，加一个严格因果零状态对照，再加一个统计不足的反例架构。三种模型中只有一个需要方法，不算三个模型普适成功。

第三关，任务泛化：提前冻结至少30条未调参内容，多个物理时间切点；至少两个真实编辑来源；分别报告底座语义成功与参考保护成功。具体数量是研究计划，不是会议录用规则。

第四关，组合闭环：至少3次前缀提交、回退和分支，检查旧显示像素而不仅是D(z_current)。对共享状态和参考像素做去重计费。

第五关，强基线：必要旧latent FP32/FP16/INT8及通用压缩；压缩后的受影响参考像素；缓存必要激活；普通统计重放；天然块边界；源输入＋生成配方重算；学习式参考压缩若预算允许。任何不可复现竞品不得填虚构数值。

第六关，资源公平：V100产主表第一行，H20使用同一算法；不得混合FP16大小与FP32误差。报告参考捕获、raw解码、恢复、序列化、校验、I/O、显存峰值和端到端成本。单次warm timing不是性能显著性证据。

第七关，质量：保护max error、RMSE与实际像素量化影响；编辑区与raw一致性；盲评边界自然度与语义目标。结构保证不能替代感知评价。

建议继续门槛：在相同失真预算下，至少两个相关架构的状态—恢复成本前沿优于强基线，且真实项目有预先声明的可观收益。若项目总收益持续只有个位数百分比且增加约一次解码，需诚实评估是否值得作为主论文；不能只放大附加状态比。

## 16. 可用与不可用的论文主张

可以说：我们研究冻结视频解码器中特定保护输出的参考状态；在明确执行条件下构造充分状态；提供切点、状态复用和失效审计；现有Cog结果支持这一结构机制。

暂不可说：首次参考统计重放、首次训练免费参考注入、首次局部非干扰、完整视频压缩36倍、9 KB重建任意视频、任意VAE/任意mask精确恢复、最小充分统计、全局最优状态、实时、跨硬件逐位一致、已经达到顶会标准。

建议工作标题：Dependency-Aware Reference States for Committed Prefix Preservation in Frozen Video Decoders。此标题是研究方向描述，不是承诺已经完成通用依赖编译。

简短定位句：Unlike approximate normalization reuse for sparse editing or tiled reconstruction, we study the conditional reference information required to preserve previously committed video outputs after permitted future-latent replacement. Any claim beyond the audited decoder execution must be supported by additional structural analysis and experiments.

## 17. 来源与核对位置

[S1] SIGE，官方论文III-B归一化预计算，实验实现说明：[论文正文](https://ar5iv.labs.arxiv.org/html/2211.02048)；[官方代码](https://github.com/lmxyy/sige)。本轮读原文，未运行官方引擎。

[S2] Tiled VAE作者技术说明：[作者Wiki](https://github.com/pkuliyi2015/multidiffusion-upscaler-for-automatic1111/wiki/%E6%8A%80%E6%9C%AF%E9%83%A8%E5%88%86)。核对快速/普通模式与统计复用。

[S3] ComfyUI-TiledDiffusion：[官方源码](https://github.com/shiimizu/ComfyUI-TiledDiffusion/blob/main/tiled_vae.py)。核对GroupNormParam.from_tile、summary及estimate_group_norm；本地抓取快照日期2026-09-22，不声称main分支是永久版本。

[S4] Huang与Belongie，AdaIN，ICCV 2017：[论文](https://arxiv.org/abs/1703.06868)。核对统计对齐与空间控制，不以其主题不同为由忽略先例。

[S5] StyleAligned，CVPR 2024：[作者项目页](https://style-aligned-gen.github.io/)。核对Q/K AdaIN、参考共享attention与无需微调。

[S6] PELC／DecFormer，CVPR 2026：[CVF正文入口](https://openaccess.thecvf.com/content/CVPR2026/html/Bradbury_Your_Latent_Mask_is_Wrong_Pixel-Equivalent_Latent_Compositing_for_Diffusion_CVPR_2026_paper.html)。核对像素等价定义与训练式latent融合。

[S7] LFV：[论文正文](https://arxiv.org/html/2608.07569)。核对C1—CM选择、fallback与往返漂移。

[S8] RefDecoder：[论文正文](https://arxiv.org/html/2605.15196)。核对reference attention、参考编码器及附录训练设置。

[S9] Mirage资产编辑：[论文正文](https://arxiv.org/html/2512.24227v1)。核对3.2.1 VAE适配、时间无关2D特征与两类LoRA。

[S10] LiveEdit：[论文正文](https://arxiv.org/html/2606.26740)。核对3.2蒸馏与3.3 AR-oriented mask cache；本轮页面为v2。

[S11] Chen等，Training Deep Nets with Sublinear Memory Cost：[论文](https://arxiv.org/abs/1604.06174)。本轮核对摘要与研究目标，不借用未复核的最优性结论。

[S12] Orlitsky与Roche，Coding for Computing，IEEE Transactions on Information Theory 47(3), 903–917, 2001：[DOI](https://doi.org/10.1109/18.915643)。仅引用带侧信息函数编码的背景，不把其渐近定理直接应用为本方法有限视频码率下界。

[S13] Wu与He，Group Normalization，ECCV 2018：[CVF](https://openaccess.thecvf.com/content_ECCV_2018/html/Yuxin_Wu_Group_Normalization_ECCV_2018_paper.html)。第6节导数从已知GN公式直接推导，应作为解释而非原始新发现。

## 18. 交付与复现

新增原型：[boundary_contract_v4.py](/Users/yizhouyang/With/20260921/m6sk/boundary_contract_v4.py)。它是完整可执行代码，使用既有V100研究环境、模型与数据，无新增训练依赖。运行命令：

```bash
/root/miniconda3/bin/python /root/viewdit/experiments/video/boundary_contract_v4.py --out /root/viewdit/results/video/boundary_contract_v4
```

本地统计：[boundary_contract_v4_stats.json](/Users/yizhouyang/With/20260921/m6sk/boundary_contract_v4_stats.json)。代码依赖torch、diffusers 0.31.0、NumPy与Pillow，使用既有环境，不建议为了该脚本覆盖原环境安装。

本报告与附录、可运行原型、源快照清单和原始统计共同交付。任何概念方案与实际代码存在范围差异时，以“当前新增原型的行为”和实际测试协议为准，不把计划当已实现。

## 19. V4实测结果

本轮48个编辑实例全部完成，V100-SXM2-32GB，FP32，128×224、33帧。每行8例，4个复用开发内容，每内容2次随机后缀替换。以下是各切点跨全部实例的最大误差，不是均值。

|首个编辑latent b|实例数|统计payload/字节|raw前缀最大变化|恢复前缀最大误差|编辑区最大误差|
|---|---:|---:|---:|---:|---:|
|3|8|0|0|0|0|
|4|8|9472|0.2585004|2.4139881e-06|0|
|5|8|0|0|0|0|
|6|8|9472|0.29839709|1.937151e-06|0|
|7|8|0|0|0|0|
|8|8|9472|0.24145566|2.6971102e-06|0|

零状态切点的24例中，原始decode已经严格保持保护前缀；不应将此记为统计重放收益。跨统计切点的24例采用9472字节payload，在本次样本上全部达到1e-5绝对误差预算。预算对应[-1,1]浮点像素尺度，不代表bit-exact，也不保证跨设备逐位一致。

保护前缀改变、模型标识改变、分块配置改变三项guard测试均拒绝复用。GN跨区导数解析式与自动微分最大差1.11e-16，数值秩2；同均值方差输入经过ReLU后得到0.5与0.353553的不同均值，反例验证通过。

本轮已完成的是多切点参考恢复和限定配置的拒绝复用检查，不是自动依赖编译、跨架构泛化、多版本提交或新的语义质量实验。统计payload仍与原V3相同，没有新增压缩倍率。
