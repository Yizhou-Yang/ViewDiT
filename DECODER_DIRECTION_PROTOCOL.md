# 候选方向协议：解码器感知的时间局部编辑

状态：2026-09-21探索协议；Wan VAE实验已写好并排队，尚无结果。不是投稿就绪结论。

## 当前结论

终端反馈+Anderson+硬回填不作为独立顶会主张。Deep Equilibrium Approaches to Diffusion Models（NeurIPS 2022）已有Anderson/扩散反演；局部编辑和过渡生成已有EditCtrl、VideoCanvas、PISCO等。此前数值改善只证明求解器可用。

## 更具体的问题

用户约束的是最终像素帧，现代模型操作的是时间压缩latent。即使DiT完全不更改保护区latent，因果解码器仍可能把前面编辑的内部状态带入后续保护帧。任务是把像素域的“允许修改/必须保留”约束转换成对实际生成表示可执行的条件，而不是仅缩放mask或提示词强度。

潜在题目：Decoder-Aware Temporal Locality in Video Editing。它是研究问题的工作标题，尚无独有性证明。

## 机制与可检验命题

因果流式解码可以写为h[t+1]=F(h[t],z[t])，v[t]=H(h[t],z[t])。这里h包含所有层缓存，而非仅首层缓存。

1. latent[z>=b]保持原值不推出像素[v>=b]保持，因为边界状态h[b]可以已改变。
2. 若恢复全部解码状态h[b]且后续latent恢复原值，则在确定性相同执行条件下，后缀输出等于原解码后缀。可用归纳证明。
3. 上述全部状态恢复在整帧时间切割场景中可能严格等价于像素拼接。若实测等价，就不能把零泄漏当作新编辑方法贡献。
4. “VAE时间依赖存在”是已有知识。真正可能有贡献的是编辑命中、时间边界、后缀保护与可重编辑性之间的定量约束，以及不依赖硬像素替换的有效控制。

## 本轮最小实验

- 官方Wan2.1 VAE，FP32，不训练，不升级当前环境。
- 4条已有Latte输出重采样为128×128，尾帧重复为17帧，仅为可执行机制探针，不是假称真实语义编辑集。
- 对5个latent时刻分别做脉冲扰动，保存每个输出帧的响应。
- 编辑第2个latent，名义对应0-based第5至8帧；后续第9至16帧应保留。
- 对照：仅回填latent、仅恢复首层缓存、恢复所有缓存、25/50/75%状态混合、像素后缀拼接。
- 必须验证流式复现与官方decode完全一致，全部输出有限。
- 必须直接检验“所有状态恢复是否等价于像素拼接”，并报告原始输入相对VAE重建误差。

## 竞争工作边界

- VideoCanvas：像素帧条件的时间索引与ICC，已讨论时间压缩导致的控制歧义；不能声称首个研究精确帧控制。
- PISCO：稀疏条件的分布保持与availability mask；不能声称首个解决视频VAE mask问题。
- RefDecoder：训练参考条件解码器来恢复细节和一致性；不能声称首个把编辑/参考信号放到解码器。
- MAGVIT：tokenizer非局部感受野与mask已讨论；不能声称首次发现token不对应独立像素块。
- 普通inpainting/输出合成：必须给同等信息与同等编辑强度，不能人为禁用强基线。

## 若机制实测成立，如何推进

将控制变量从单纯编辑latent拓展为编辑终止后的补偿latent，利用因果性确保未来补偿不会改变已生成核心区。优化真实decode输出的后缀残差，而不是单步pred-x0。基线包含无补偿、统一衰减编辑、全量latent优化、状态重置与像素拼接。

用局部线性模型J_P表示补偿latent到保护区像素的响应，J_E表示对编辑区影响。纯因果且补偿位于核心区之后时J_E=0是结构性质，不是近似小值。可用矩阵自由梯度或小型响应基求解J_P c≈-r_P，目标同时限制补偿范数与过渡不自然性。

重要：一般最小二乘、Adam、nullspace不新。论文成立需要证明这种结构分离提供以往方法没有的帧级可控性、可编辑latent输出或跨模型规律。失败求解不能当作不可达证明；只称在当前控制子空间/预算内不可达。

## 继续/停止条件

有明显解码后缀串扰并且可用补偿保持核心、显著降低串扰，则选择该方向继续开发；仅状态重置实现零泄漏但与像素拼接相同，不足以升级为方法论文。正式结果必须扩展到真实视频、至少两种VAE、一个完整语义编辑骨干以及强合成基线，评估视觉质量和再编辑兼容性。
