# 七方向 + FastVMT 式多组件组合验证报告（2026-09-24）

设置：Latte-XL/2（FFS，EMA），DDIM-50，fp16，16 帧。同预算比较（FFE = 已计算 block / 28）。
指标：最终 latent 与 Full DDIM-50 的 relL2；解码后 LPIPS(alex)。配对 per-seed 统计。
门槛：相对同预算 Taylor-1（B1）的中位下降 ≥25%，win ≥0.75，均值 >2·SEM，16 seed。

## 1. 单组件七方向筛选（seed 9000–9015，15 FFE）

结果目录 `results/video/seven_dir_20260924`。没有方向过门槛，Taylor-1（relL2 中位 0.1475，LPIPS 0.0152）仍是最强单组件。
- D1 非均匀调度（Taylor-1s）、D6 开环学习系数、D7 DPM++2M：均劣于 B1。
- D2 回溯修正：relL2 中位 +6%，但 LPIPS 劣化 57%（全权重修正过冲）。
- D3/D4 特征级残差复用、D5 仅空间步复用 temporal 残差：大幅劣化（LPIPS 劣化 3–18 倍）。结论：Latte 的块级/时空结构冗余在 15 FFE 预算下不可利用。

## 2. 组合实验（seed 9000–9015，15 FFE，14 个组合）

结果目录 `results/video/seven_combo_20260924`，组件池：T1、T2、回溯修正 R(w)、调度 SC(p)、仅空间步 S、深层残差复用 DR、闭环学习系数 L。

| 组合 | relL2 降幅(中位) | win | LPIPS 降幅(中位) | win | 门槛 |
|---|---|---|---|---|---|
| T1+SC1.25+R0.5 | +18.9% | 0.88 | +33.4% | 0.81 | LPIPS 过 |
| T1+R0.5 | +14.8% | 0.75 | +29.9% | 0.75 | 否 |
| L_closed+R0.5 | +18.7% | 0.69 | +36.7% | 0.75 | 否 |
| 含 S 或 DR 的全部组合 | -36% 到 -119% | ≤0.31 | 劣化 | — | 否 |

## 3. Held-out 复现（seed 9100–9115，10/15/20 FFE，w 与 p 网格）

结果目录 `results/video/seven_deep_20260924`。第 2 节中挑出的最优配置未能复现：
- `p1.25_w0.5@15`：relL2 中位 +9.5%，LPIPS +3.9%，win 0.56，均值为负（-20%±26%）。
- 最稳的是 `p1.0_w0.25`：三档预算下 LPIPS 中位 +14%~+29%，win 0.81–0.88，但 relL2 只改善 6–13%，没有一档过门槛。
- 大 w 在个别 seed 上会过冲（均值为负，SEM 很大），说明修正量需要做 per-seed 自适应，全局固定权重不可靠。

## 4. 结论

1. 在 Latte-XL/2、10–20 FFE 预算下，FastVMT 式组合（缓存预测 + 回溯修正 + 调度 + 结构复用）没有找到能在 held-out 上显著超过 Taylor-1 的方案。第 2 节的"过门槛"属于选参过拟合。
2. 唯一稳定为正的组件是低权重回溯修正（w≈0.25），属于小幅改善（LPIPS 约 15–30%，relL2 约 5–10%）。它在机制上与 Z-Cache 的回溯插值修正撞车，novelty 不足。
3. 结构冗余（时空块残差、深层残差）的 7 个组合全部显著劣化。在 Latte 上，这条"识别结构冗余"的路径目前已被证伪。
4. 建议：Latte 上的缓存加速方向不要再作为论文主线投入；需要换模型（Wan2.1 / CogVideoX 这类更大、冗余更多的 DiT）或换目标（质量提升而非逼近 full）。
